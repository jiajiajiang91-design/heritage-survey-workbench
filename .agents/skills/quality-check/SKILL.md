---
name: quality-check
description: 改完文档或代码要收口时使用。跑机器检查（字符规范、跨文档与文档代码之间的引用、实施单元与路线图的状态一致性、类型、测试、演示矩阵、演示包一致性），并按结果更新该更新的状态文件。用户要求校验、检查一致性、单元收口、提交前自检时读取。
---

# 质量检查

## 目标

机器能判定的规则由脚本执行，不靠人逐条记。脚本报出的每一条都要处理完再收口，不留静默差异。

## 使用顺序

改完东西之后：

```
node .claude/skills/quality-check/scripts/run-all.mjs
```

单元收口或提交前，追加测试、演示矩阵与演示包一致性：

```
node .claude/skills/quality-check/scripts/run-all.mjs --full
```

也可以单独跑其中一步：

| 脚本 | 查什么 |
|---|---|
| `check-docs.mjs` | 禁用字符、中英文空格、markdown 链接、文档里写的仓库路径、源码里写死的文档路径 |
| `check-state.mjs` | 单元三件套是否齐、验收判定进度、路线图与目录事实是否一致、索引与实际文件是否一致、pm-context 指针是否可达 |
| `check-test-regex.mjs` | 测试里有没有用模板字符串拼正则 |
| `.claude/scripts/sync-agent-adapters.mjs --check` | 根规则、Codex 角色和通用 skill 是否与 `.claude` 唯一维护源一致 |

`check-docs.mjs` 默认跳过 `文档/99_历史归档` 与 `文档/05_验证证据`，两者刻意保留当时状态的旧路径。加 `--all` 连它们一起查，加 `--verbose` 展开建议核对项。

## 新写的测试要实测一次能不能失败

写完一条测试，先动一处让它应该失败的地方，跑一次确认真的红，再改回来。绿灯本身不说明测试在查东西。

2026-08-22 单元 06 里连续出过两次空转测试，都是同一个原因：模板字符串会先吃掉一层反斜杠，`` new RegExp(`var\(...\)`) `` 里的 `\(` 变成 `(` 被当成分组，`\s` 变成 `s`，正则永远匹配不到，六条档位校验全绿而什么都没查。`check-test-regex.mjs` 现在挡住这一类写法，但它只挡这一类，别的空转形式仍要靠实测发现。

## 结果分两级

**必须修复**：链接失效、路径不存在、源码引用的文档路径不存在、禁用字符。这类会让别人点不开、让构建或测试失败，收口前清零。

**建议核对**：中英文空格、变更对照表里的旧路径。这类按文件聚合显示，历史笔记里的存量不必回头改，新写的内容要改对。

## 检查通过之后要更新什么

脚本只报差异，不改写文档，因为状态描述是语义内容。按报出的差异更新这几处：

| 情况 | 更新位置 |
|---|---|
| 单元验收全部判定完毕 | 未触发 `pm-context` 确认门槛时，直接更新路线图对应阶段状态。单元留在实施区，阶段收尾时整批归档 |
| 单元状态变化 | `文档/04_实施单元/00_路线图.md` 的对应阶段行 |
| 权威文档改名或新增 | `.claude/context/pm-context.md` 指针表与 `文档/00_索引.md` |
| 文档路径变动 | 源码里写死该路径的地方，`check-docs.mjs` 第 5 项会报出来 |
| `.claude` 下角色、skill 或项目规则变化 | 运行 `node .claude/scripts/sync-agent-adapters.mjs` 后再检查 |

## 边界

脚本查的是形式，不是内容。以下要人判断，通过不等于合格：

- 每章第一句是不是结论，数据有没有标来源，假设有没有写验证方式
- 验收条件本身是否成立，而不是表格填满了
- 结论与证据是否对得上

技术检查通过不代表专业成果合格，这条口径与产品一致。
