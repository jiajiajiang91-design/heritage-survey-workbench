# 单元复盘清单

单元验收通过后、归档之前做一次，五个问题都要回答。回答产生的改动直接落到对应文件，不另写复盘报告。

## 五问

**一、路线图的下一阶段还成立吗。** 本单元的产出是否改变了后续顺序或前置条件。需要调整时更新 `文档/04_实施单元/00_路线图.md` 的对应阶段行。

**二、规范要不要更新。** 本单元是否触及了 CLAUDE.md、协作体系、界面视觉规范或质量基准里没写清的地方。要更新就改到对应文件，不留在对话里。

**三、本次发现的新规则要不要写回文档。** 实施中确定的判断口径，例如取值优先级、失败时的处置、边界情况的做法，是否已经写进核心 PRD、界面文档或质量基准。只写在单元规格里的规则，归档后就找不到了。

**四、有没有技术债要进路线图。** 本单元为了收口而绕开的问题，写成路线图的一项，不留在注释或对话里。

**五、有没有重复流程该沉淀为脚本。** 本单元里手工重复三次以上的检查，加进 `.claude/skills/quality-check/scripts/`。

## 归档

五问回答完毕且验收全部通过后，直接更新路线图对应阶段的状态。单元留在 `文档/04_实施单元/`，阶段收尾时整批移入 `文档/99_历史归档/`。
