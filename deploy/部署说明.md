# 公网展示部署说明 v1.0

线上应用部署在 `heritage.jiajiajiang.com`，前端、后端、AI 与 CAD 功能均由同一台服务器提供。

目标环境是腾讯云上海轻量应用服务器，配置为 4 核、4 GB 内存，操作系统为 OpenCloudOS，公网 IP 为 `49.235.158.54`。域名 `jiajiajiang.com` 已备案。

Nginx 提供 HTTPS 和静态前端。`/api` 反向代理到本机 Node 服务，Node 服务按需启动 Python CAD 进程。Kimi 密钥只保存在服务器的 `/etc/gujian/server.env`。

## 一次性开通步骤

一次性开通需要完成域名解析、证书、防火墙、服务安装和密钥配置。

1. 在 `jiajiajiang.com` 的 DNS 解析中增加 A 记录。主机记录为 `heritage`，记录值为 `49.235.158.54`。
2. 在腾讯云控制台申请 `heritage.jiajiajiang.com` 的免费 SSL 证书。签发后下载 Nginx 格式证书，并把两个文件放入服务器的 `/etc/nginx/certs/`。
3. 在轻量应用服务器防火墙中放行 TCP 80 和 443 端口。
4. 服务器上以 root 执行 `deploy/setup-server.sh`（代码用 rsync 推上去时先 `export GUJIAN_SKIP_CLONE=1`）。
5. 编辑 `/etc/gujian/server.env` 并填写 `KIMI_API_KEY`，然后执行 `systemctl restart gujian-server`。

## 日常更新

日常更新必须在测试通过并完成演示包重建后执行。

```bash
rsync -az --delete --exclude .git --exclude node_modules --exclude .venv --exclude "apps/server/.data" ./ root@49.235.158.54:/opt/gujian/app/
ssh root@49.235.158.54 "chown -R gujian:gujian /opt/gujian/app && sudo -u gujian env HOME=/home/gujian PATH=/usr/local/bin:/usr/bin:/bin bash -c 'cd /opt/gujian/app && pnpm install --frozen-lockfile && pnpm run build' && systemctl restart gujian-server"
```

服务器部署目录没有 `.git` 时，可以运行部署目录内的更新脚本。参数必须是已经推送到 GitHub 的分支名。当前展示版本使用 `codex/professional-workbench-rebuild`：

```bash
bash /opt/gujian/app/deploy/update-from-github.sh codex/professional-workbench-rebuild
```

脚本会保留 `apps/server/.data`，重新构建前后端，更新 Nginx 配置并重启服务。执行前必须确认目标目录是 `/opt/gujian/app`。

## 公网防护

公网模式由 `GUJIAN_PUBLIC_HOST` 启用。服务端只允许指定域名，会话 Cookie 使用 Secure 属性。

助手对话和生成作业分别计数，并同时设置单 IP 与全站每日限额。额度用完后，接口返回 HTTP 429 和中文说明。计数在东八区每日零点清零。默认额度和可配置项见 `server.env.example`。

## 上线记录（2026-08-25 完成）

生产环境已经完成 HTTPS、展示项目、AI、模型生成和图纸生成检查。

- 两个公开展示项目可以载入。
- 助手通过 Kimi 回答项目通面阔为 9600 mm。
- CadQuery 完成一次模型重新生成。
- 制图服务按成果目录完成一次图纸生成，新成果显示为待签发状态。
- TrustAsia DV 证书有效期至 2026-11-23。到期前需要重新申请免费证书，并替换 `/etc/nginx/certs/` 下的两个证书文件。

演示项目包不包含服务器的模型作业记录。新服务器上的作业记录为空时，直接选择“重新出图”会提示先生成模型。用户应先选择“重新生成模型”，完成后再生成图纸。

## 验收清单

上线验收覆盖服务状态、项目载入、AI、CAD 作业和额度限制。

1. `curl -s https://heritage.jiajiajiang.com/api/status` 返回 `modelConfigured: true`。
2. 手机蜂窝网络打开站点，两个公开演示项目自动装载。
3. 向助手提问“这个项目的通面阔是多少”，回答为 9600 mm。
4. 选择“重新出图并检查”，作业在数分钟内完成。
5. 同一 IP 的连续对话超过单日上限后，页面显示“额度已用完”。

## 已知限制

当前部署适合公开产品展示，不满足多人生产环境的可用性要求。

- 单机单进程：CAD 作业并发为 1，第二个访客的生成请求会排队。
- 限额计数在进程内存里，重启服务当日计数清零（展示场景可接受）。
- 更新演示包后需要重新执行 rsync。已有访问者还需要在项目列表选择“更新演示项目”。
