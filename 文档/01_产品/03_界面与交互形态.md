# 05 · 界面与交互形态

## 一、形态判断

产品采用工作台形态：左侧项目与任务，中间随任务切换的工作区，右侧常驻 AI 助手。用户在助手里说话，在工作区里看证据和改结果。

用户面对一个助手，服务端按任务路由模型。普通用户无需选择模型品牌，模型与供应商信息留在内部选型记录中。

三栏结构将对话、任务状态、数据和证据放在同一操作环境。用户可查看处理进度、定位依据，并在原图上修正结果。

结构依据见 [02 用户旅程](02_用户旅程.md) 的八项原型约束，重点满足自动推进、责任停靠、原图修正和分支并行。

交互参考 VS Code 和 Cursor 的工作台结构。中间工作对象为照片、构件数据和图纸，因此需支持图像框选和数据联动。

视觉形态、组件规格与状态表达见 [02 界面视觉规范](../02_技术/02_界面视觉规范.md)，本文只定义结构与交互。

## 二、整体结构

**图 1：三栏工作台线框**

```
┌──────────────┬────────────────────────────────────┬──────────────────┐
│ 左栏  240px  │ 中栏  工作区（自适应）              │ 右栏  380px      │
├──────────────┼────────────────────────────────────┼──────────────────┤
│              │  ┌──────────────────────────────┐  │ ┌──────────────┐ │
│ 项目         │  │ 视图标签                      │  │ │ 当前状态条    │ │
│  高都玉皇庙   │  │ 任务卡│资料│构件│图纸│检查    │  │ │ 正在识别构件  │ │
│   主殿正立面  │  └──────────────────────────────┘  │ │ 已处理 12/34 │ │
│              │                                    │ └──────────────┘ │
│ 任务进度      │  ┌─────────────┬────────────────┐  │                  │
│  建立任务 ✓   │  │             │                │  │  消息流          │
│  整理资料 ✓   │  │  数据视图    │   证据视图      │  │  ┌────────────┐ │
│  核对构件 ●   │  │             │                │  │  │ 助手：资料   │ │
│  记录现状 ○   │  │  构件清单    │   原始照片      │  │  │ 够用了，我   │ │
│  生成图纸 ○   │  │  47 项       │   可框选        │  │  │ 开始识别    │ │
│  检查签发 ○   │  │             │                │  │  └────────────┘ │
│  交付归档 ○   │  │  点击行 ←──→ 高亮对应位置      │  │  ┌────────────┐ │
│              │  │             │                │  │  │ 我：这里漏   │ │
│ 待办 3        │  └─────────────┴────────────────┘  │  │ 了一个雀替   │ │
│  存疑构件 2   │                                    │  │ [引用位置]  │ │
│  等我签发 1   │                                    │  └────────────┘ │
│              │                                    │                  │
│              │                                    │ ┌──────────────┐ │
│              │                                    │ │ 输入框        │ │
│              │                                    │ │ 说话 / 拖文件 │ │
│              │                                    │ └──────────────┘ │
└──────────────┴────────────────────────────────────┴──────────────────┘
```

三栏职责见表 1。

**表 1：三栏职责**

| 区域 | 职责 | 边界 |
|---|---|---|
| 左栏 | 显示项目、任务进度、待办和当前停靠位置 | 具体数据与编辑操作放在中栏 |
| 中栏 | 显示当前对象，支持在证据上直接操作 | 流程推进由编排层控制 |
| 右栏 | 承载对话、系统状态和修正指令 | 详细数据只在中栏呈现 |

## 三、页面与视图

页面分两层：项目级页面不属于任何一栋建筑，工作区视图随当前建筑的任务切换。

**表 2：工作区视图**

2026-08-21 起视图与任务阶段一对一：每个阶段独占自己的视图，不再出现两个阶段共用一组页签。原来的图纸与检查一分为二，交付包按代码里的说法改称代理交付；2026-08-23 实施单元 09 按界面用语表改称成果归档。裁决见 `../05_验证证据/14_界面重做/缺口与疑点裁决记录.md` 第三节第 7 条。

2026-08-22 起工作台的视图名与本表一致。此前实现侧有五处历史命名（任务要求、项目资料、测量与尺寸依据、对象与构件、模型运行与费用），实施单元 06 第四组统一到本表，视图 id 未动。`stage-map.test.ts` 逐行比对本表与代码，改一处不改另一处会失败。

| 阶段 | 视图 | 左半区 | 右半区 | 对应功能 |
|---|---|---|---|---|
| 01 建立任务 | 任务卡 | 成果类型、比例、规范、交付项、参与人员与检查要求 | 任务书原文，可点到出处 | F01 |
| 02 整理资料 | 资料清单 | 每份资料的类型、拍摄部位、覆盖情况、可用性 | 选中资料的原图 | F02 |
| 03 核对实测 | 实测基准 | 控制点、实测值、精度、来源与责任 | 手写草图照片，数值可点回原处 | F05 |
| 04 核对构件 | 构件清单 | 构件表，按部位分组，存疑项置顶 | 主图，带位置框 | F04、F08、F11 |
| 04 核对构件 | 三维模型 | 构件树、几何版本、未知几何标记 | 三维视图 | F07 |
| 05 记录现状 | 现状记录 | 空间关系、构造连接、可见残损、待复查区域 | 对应部位照片 | F06 |
| 05 记录现状 | 问题队列 | 问题按原因分组，附证据与影响范围 | 选中问题的对应证据 | F09、F10 |
| 06 生成图纸 | 图纸样式 | 构件画法、标注规则、图签、附表选择 | 样式应用后的局部预览 | F12 |
| 06 生成图纸 | 成组图纸 | 各图的图幅、视图与图签配置 | 图纸预览 | F13 |
| 07 检查签发 | 检查与签发 | 检查结果、成果等级与使用限制，按后果排序 | 图纸，问题标在对应位置 | F14、F15 |
| 08 交付归档 | 成果归档 | 文件清单、版本、限制条件、签发状态、查看与导出权限 | 选中文件的预览 | F16、F17 |

**表 3：项目级页面**

| 页面 | 内容 | 对应功能 |
|---|---|---|
| 项目列表 | 全部项目、各自停靠位置、新建与导入导出入口 | F03 |
| 模型运行与用量 | 真实调用的耗时、token 用量与按公开单价算得的费用、识别候选区、人工基线 | 横向支撑 |
| 修改历史 | 按时间倒序列出项目里发生过的每一次写入，含时间、操作人、动作、改了哪些对象、为什么改 | F11、F18 |

左栏按八个阶段导航，不平铺视图。阶段三态为当前、已完成、未开始，判据只看该阶段自己有没有数据，不按序推进。本产品允许资料先到现状后补，按序推进会把已经做完的后续阶段显示成未到，属误报。中栏的页签行按 v4 的形式始终显示：单视图阶段显示一个页签，八个阶段里有三个含两个视图。页签行常驻让每屏的页头结构一致，也给助手与测试一个固定的视图切换入口（2026-08-22 实施单元 08 起）。

三个项目级页面的入口在顶栏右侧，不进左栏。进入后只显示中栏：左栏是阶段导航，项目级页面不属于任何阶段；助手按当前阶段说话，这三个页面没有对应的阶段上下文。页面上方有一条返回按钮，回到进入之前那个视图。

全部视图采用左侧数据、右侧证据的结构。点击数据后，证据区跳到对应位置并高亮。

修改历史列为项目级页面而不是视图，因为它横跨全部视图：一次框选修正同时动到构件清单与排除记录，一次尺寸确认动到实测基准，按视图切分会把同一件事拆散。表 10 里多个动作的留痕去处都写着修改历史，此前工作台没有一处界面承接，数据层留痕齐全而人看不到。这一项由实施单元 02 的验收第 3 条驱动补上。

现状记录独立于构件清单，因为形制测绘与残损勘察分属两套标准体系：几何测绘依据 [CH/T 6005-2018《古建筑测绘规范》](https://std.samr.gov.cn/hb/search/stdHBDetailedCNF?id=8B1827F24F89BB19E05397BE0A0AB44A)，残损与结构安全依据 [GB/T 50165-2020《古建筑木结构维护与加固技术标准》](https://www.gongbiaoku.com/book/icz19014nxx)。两项工作可能由不同资质人员承担，责任边界不能合并。适用范围为木结构古建筑。

问题队列独立于图纸与检查：前者处理出图之前的数据问题，属 M3；后者处理出图之后的图面问题，属 M4。两类问题的处置时机和责任人不同。

图纸与检查原定义为一个视图，2026-08-21 拆成两个：成组图纸属阶段 06，检查与签发属阶段 07（原名检查与资格，2026-08-24 随用语层改名：资格对测绘从业者是人员资质的意思，这一屏说的是成果能否签发交付）。拆分理由是两者的责任人不同，出图由测绘人做，签发由项目责任人做，合成一个视图会让阶段导航失去意义。用户认知上仍是旅程第六步的同一段，助手在两者之间切换时说明当前在哪一步。

正式出图前需在图纸样式视图确认画法、标注规则和图签。

## 四、右栏的构成

右栏自上而下分三段（见表 4）。

**表 4：助手区构成**

| 区段 | 内容 | 作用 |
|---|---|---|
| 状态条 | 当前正在做什么、进度、用了哪些资料 | 让用户随时知道系统在处理什么，避免黑箱等待 |
| 消息流 | 用户与助手的往复消息，含引用的位置、文件和构件 | 承载全部协作过程，可回溯 |
| 输入区 | 文字输入、文件拖入、语音转文字（后续版本） | 接收用户指令和资料 |

助手消息分四类，界面上要能区分（见表 5）。

**表 5：助手消息类型**

| 类型 | 例子 | 界面表现 |
|---|---|---|
| 进度告知 | 我开始识别构件 | 普通消息，可折叠 |
| 结果汇报 | 识别完成，47 个构件，6 项存疑 | 带结果卡片，可点入工作区 |
| 停靠提问 | 没有实测基准，你是补测还是先出草图 | 突出显示，附可选项，未回答前该分支不继续 |
| 风险提示 | 这样出来的图不能作为正式成果 | 与提问同级，不可折叠 |

第 1 代使用一个通用助手。真实项目若证明识别、制图和审核需要独立角色，再评估多助手形态。

## 五、五个关键交互

### 5.1 框选加一句话

框选修正对应用户旅程第四步。

流程：用户在中栏右半区的照片上拖出一个框，右栏输入区自动带上这个位置的引用，用户打字说明问题，发送。助手定位到对应构件或空白区域，执行修改，中栏数据同步更新并高亮变化处。

系统根据框选位置关联构件编号和表格行。

支持的说明方式见表 6。

**表 6：框选后可以说的话**

| 用户说 | 系统理解为 |
|---|---|
| 这里漏了一个雀替 | 在该位置新增构件，类型为雀替 |
| 将这个构件从雀替改为替木 | 修改该位置构件的类型 |
| 这个框位置不对，应该往左 | 调整位置框，重算相邻关系 |
| 这里没有构件 | 删除该条，记入本项目排除记录 |
| 这块被树挡住了，看不清 | 标记为不可见部位，进入待补拍清单 |

### 5.2 数据与证据双向联动

点击左半区任一条数据，右半区跳到对应证据并高亮。在右半区选中任一位置，左半区自动滚动到对应数据行。

这条规则覆盖全部工作区视图。点击图纸构件后，可查看原始照片、尺寸依据和修改记录。

### 5.3 停靠点

流程按状态自动推进，并在四类情况下停靠（见表 7）。

**表 7：四类停靠点**

| 停靠原因 | 例子 | 界面表现 |
|---|---|---|
| 证据不足 | 没有实测基准、关键部位被遮挡 | 右栏提问，左栏该步骤标为等待确认 |
| 结论存疑 | 铺作形制有两种可能、与旧图纸冲突 | 转为待办，存疑项在清单中置顶 |
| 规范选择 | 不可见部位如何在图上表达 | 右栏列出可选做法及各自后果 |
| 正式签发 | 成果交付前 | 只有责任人可以操作，其他角色不显示该按钮 |

停靠只影响相关分支。两处铺作存疑时，其余 45 个构件照常进入出图。

### 5.4 随时插话

用户可以在任何时刻打断当前流程，说明新的要求。例如识别进行到一半时说“先别管构件了，帮我看看资料够不够出侧立面”，助手暂停当前任务，切到资料视图回答，回答完询问是否继续。

切换任务时保留已完成结果和当前状态。

### 5.5 三种状态的可见区分

每一条数据都标明它的来源（见表 8）。

**表 8：数据状态**

| 状态 | 含义 | 建议表现 |
|---|---|---|
| 实测依据 | 有现场实测记录支持 | 实心标记，可点开查看原始记录 |
| AI 推测 | 由模型判断或按对称关系推算 | 空心标记，鼠标悬停显示推断依据 |
| 人工确认 | 经操作用户或责任人判断 | 带姓名标记，可点开查看理由 |

正式成果中的每条数据均需标明来源。无实测依据的尺寸需在图纸和交付说明中标注。

## 六、交互假设

以下判断尚未经真实用户验证（见表 9）。

**表 9：待验证假设**

| 假设 | 验证方式 |
|---|---|
| 用户愿意用说话代替填表发起任务 | V1B 真实项目中记录用户实际使用哪种方式 |
| 框选加一句话比在表格里改更快 | 同一批修正任务用两种方式各做一遍，计时对比 |
| 用户能接受系统自己往前跑 | 观察用户是否频繁打断，是否要求增加确认步骤 |
| 三种状态标记能被正确理解 | 让未参与设计的专业人员解释标记含义 |
| 停靠点的数量合适 | 统计停靠次数与用户等待时间，判断是否过多或过少 |

验证未通过时，依据使用记录调整交互步骤、信息密度和停靠规则。

## 七、AI 动作清单

动作目录当前含 14 个已定义动作（第 1 代 6 个、第 2 代 7 个、第 3 代提前 1 个），另有 7 个第 3 代动作仅定义占位。

每个动作的实现状态由代码登记表 `packages/domain/src/assistant-action-delivery.ts` 定义，表 10 的实现状态列取自该表，本文不另行叙述。状态取两值：`executable` 表示前端执行体真实可跑、端到端可用、投影进模型可见目录；`definedOnly` 表示只有目录定义与参数校验，前端缺执行体，不投影给模型。登记表与目录一一对应、与前端执行体一致，两侧各有测试守着，文档说已交付而代码没做的情况会在测试里失败。模型经意图分发选中动作，参数校验失败回模型重试一次，二次失败落关键词匹配；停靠、退回和签发仍由固定流程与责任人控制。

通用约定（适用全部动作）：

1. 模型可见字段只有名称、描述、参数定义三项，确认级别、留痕方式等执行字段不进模型请求。
2. 参数进入执行后不可改写，留痕、界面展示与实际执行使用同一份参数。
3. 失败分级：参数不合规时将结构化错误回给模型重试一次，二次失败落关键词匹配，模型服务不可用直接走关键词；均不中断对话。清单外动作按未知动作返回。
4. 高成本动作（生成三维、生成图纸）执行前展示预计耗时与用量，执行中可取消，失败不自动重试。
5. 模型可见目录只投影已实现动作，环节切换不增删目录，不可用动作由前置条件拒绝并说明原因。

通用约定与下方确认规则借鉴 deepseek-harness 官方子系统文档（tools、approval、plan，2026-08 研读）；重试次数上限、关键词退路与预估展示为项目自定。

**表 10：动作清单**

| 动作 | 实现状态 | 触发方式 | 前置条件 | 确认级别 | 留痕 |
|---|---|---|---|---|
| 切换视图 | executable | 说要看什么 | 无，浏览不设锁 | 直接执行 | 无 |
| 定位证据 | executable | 说看某构件的照片或依据 | 目标对象存在 | 直接执行 | 无 |
| 查询作业进度 | executable | 问进度、状态 | 无 | 直接执行，只读 | 并入调用记账 |
| 回答问题 | executable | 其余情况，兜底动作 | 综合分析任务路由可用 | 只读回答 | 调用记账 |
| 生成修改建议 | executable | 说要改什么、补什么、推测什么 | 修改建议任务路由可用 | 逐条确认后生效，程序先按规则表做合理性核对，超容差附警示 | 修改历史（理由、操作人、时间）加模型原文 |
| 框选修正 | executable | 在照片上框选加一句话 | 无 | 新增和遮挡标记直接执行并留痕；删除进排除记录 | 修改历史与排除记录 |
| 重新识别 | executable | 说重新识别，或对识别结果提出疑问 | 构件识别任务路由可用 | 直接执行，结果标 AI 实时，需人工核对 | 调用记账含模型原文 |
| 推进流程 | executable | 说继续 | 当前环节停靠事项已处理完 | 直接执行；停靠事项未清时拒绝并说明原因 | 流程状态 |
| 运行数据检查 | executable | 说检查 | 无 | 直接执行，只读 | 检查记录 |
| 生成三维模型 | executable | 说生成、更新三维 | 项目已导入且构件库非空 | 确认后执行，确认时展示预计耗时与用量 | 作业记录（发起人、耗时、用量） |
| 生成图纸 | executable | 说出图 | 存在几何版本 | 确认后执行，确认时展示预计耗时 | 作业记录 |
| 导出成果 | executable | 说导出 | 图纸已生成；交付级导出需证据等级满足，不满足拒绝并说明 | 普通导出直接执行；交付级确认后执行 | 导出清单与调用记账 |
| 起草交付说明 | executable | 说写交付说明 | 存在可交付内容 | 生成草稿，逐条确认 | 修改历史 |
| 解析任务书与盘点资料（第 3 代提前） | executable | 说解析任务书、盘点资料 | 存在未解析资料 | 结果逐条确认 | 调用记账 |

**表 11：动作接口契约**

| 动作 | 参数定义 | 结果结构 | 失败行为 |
|---|---|---|---|
| 切换视图 | view（表 2 与表 3 页面枚举，必填）、target（构件或资料引用，可选） | 当前视图与聚焦对象 | 视图名无法映射按通用失败分级 |
| 定位证据 | ref（构件号或自然语言描述，必填） | 数据行、证据引用、高亮位置 | 找不到对象转回答问题兜底 |
| 查询作业进度 | jobType（可选，默认全部活动作业） | 作业列表（阶段、已耗时、预估） | 无活动作业时如实说明 |
| 回答问题 | question（必填） | 回答加引用（构件、资料、位置） | 兜底动作无再退路 |
| 生成修改建议 | subjectRef（必填）、changeType（参数修改、类别修改、标记存疑、标记不可用、标记不可见、新增、删除）、payload（按类型定义） | 建议清单，每条含字段、原值、新值、依据、核对结论 | 参数不合规按通用分级；核对不通过仍出建议但附警示 |
| 框选修正 | instruction（必填）。位置不进模型参数：模型看不到那张照片，要它给坐标只能编。位置由客户端选区随回合上送，服务端在派发前注入 | 修正条目加相邻关系重算摘要 | 位置无法关联构件时向用户提问 |
| 重新识别 | scope（可选，默认当前视图对象集） | 识别结果集，命中与存疑分列 | 模型服务不可用提示稍后再试 |
| 推进流程 | 无 | 起止环节 | 前置不满足时结构化拒绝理由交模型组织说明 |
| 运行数据检查 | scope（可选，默认当前环节） | 检查项、结论、停靠事项结构化列表 | 按通用分级 |
| 生成三维模型 | note（可选备注） | 新版本号、实体数、耗时 | 作业失败结构化原因交模型组织说明，不自动重试 |
| 生成图纸 | views（可选，默认全套） | 图纸目录与版本关联 | 作业失败结构化原因交模型组织说明，不自动重试 |
| 导出成果 | format（格式枚举）、scope（范围） | 导出清单与哈希 | 前置不满足结构化拒绝 |
| 起草交付说明 | 无 | 草稿含三要素：尺寸依据、限制条件、未确认项 | 按通用分级 |
| 解析任务书与盘点资料（第 3 代提前） | scope（可选，资料 ID 列表，缺省全部未解析资料） | 候选事实清单供逐条确认 | 按通用分级 |

**表 12：第 3 代动作（仅定义占位，未实现前不进模型目录）**

| 动作 | 触发方式 | 确认级别 | 留痕 |
|---|---|---|---|
| 录入实测记录 | 拖入草图照片说录入 | 逐条确认 | 修改历史 |
| 转办待办 | 说转给某人 | 确认后执行 | 待办记录 |
| 降级出草图 | 资料不足时选择降级 | 确认后执行 | 降级决定（决定人、时间、理由） |
| 退回重做 | 圈出问题说退回 | 确认后执行，先展示影响范围 | 退回记录 |
| 变更任务参数 | 说换比例、加立面等 | 确认后执行，先展示影响 | 任务卡历史 |
| 设置图纸样式 | 说改画法、版面 | 逐条确认 | 样式记录 |
| 存入构件库 | 责任人确认判断后选择入库 | 责任人确认 | 词表条目（确认人、理由、时间） |

确认机制遵守四条规则：

1. 确认结果是封闭集合：允许一次、拒绝、用户撤回、通道不可用，除允许一次外全部按拒绝执行。确认界面未弹出、用户关闭页面、程序异常均不放行动作。
2. 一次确认只对当前一条生效，不存在本次同意后续默认同意的隐式升级。
3. 每次确认留两条成对记录：询问一条、决定一条，同一确认号关联。程序中断时留下有询问无决定的可检测状态。
4. 确认界面引用已展示的动作卡片，不另行复制参数副本。

浏览引导与写入控制分两层，互不读写：浏览环节的引导（该看什么、下一步去哪）是软性提示，不锁定任何动作、不留痕；写入、推进、签发是硬性确认层，走确认规则与留痕。环节切换不改变动作清单，动作可用性只由前置条件表达，不可用时拒绝并说明原因。

模型输出先经过字段白名单、编号检查、标准字段映射和合理性校验。数量、状态和来源由程序统计，模型负责组织说明。对话与按钮产生的修改使用同一套来源标记和历史记录。固定说法交给规则程序，表达多变且需要语义理解的任务交给模型。

任务分类、模型适配和选型方法见 [技术架构](../02_技术/01_技术架构.md) 第 7 章。

识别任务读取项目构件库和排除记录，复用已确认形制，并过滤已排除的误识别，对应问题审计第 47 项（见 [问题审计](../99_历史归档/01_文档历史版本/重分类归档_2026-08-16/13-1_AI视角业务问题审计.md)）。

界面承接的用户旅程见 [02 用户旅程](02_用户旅程.md)，AI 与人工的分工依据见 [AI 与制图能力边界](../06_研究底稿/01_AI与制图能力边界.md)。

## 八、界面用语表

界面上的词以本表为准。左列是代码与文档里的领域词，中列是界面上的说法，右列是用法。领域词只留在代码、文档与开发者可见的详情里，不出现在正文。2026-08-23 实施单元 09 起生效，`wording` 测试扫描工作台源码里的界面字符串，出现禁用词即失败。

**表 13：界面用语**

| 领域词 | 界面说法 | 用法 |
|---|---|---|
| 证据（evidence） | 资料；说明某条结论的来源时写"依据" | 资料清单、资料原件、所依据的资料。证据一词不出现 |
| 证据预览、照片证据、基准证据 | 资料原件、对应照片、实测依据 | 右侧卡片标题按屏分别取 |
| 阻断（blocker） | 不能正式交付的原因；单项用"不通过" | 列表标题写"还差什么"，状态标签写"不通过"，数量写"N 项不通过" |
| 阻断码（`geometry.role.*`、`UNKNOWN:*` 等） | 一句中文说明 | 一律经 `describeBlocker` 译成句子，代码不出现 |
| 代理成果、代理交付、代理交付草案 | 待签发成果、成果归档、归档草案 | 视图名改为成果归档；签发后称已签发成果 |
| 正式资格、正式交付资格 | 能否正式交付 | 签发前写"签发后可正式交付"，签发后写"可正式交付" |
| 未获资格、已生成未获资格 | 待签发、已生成，待签发 | 成果状态标签 |
| 未达专业样板等级、L1 | 不作为样板 | 只在签发记录未认定样板等级时显示一句"不作为专业样板使用" |
| 未知项（unknown） | 待确认部位 | 三维模型与构件清单的计数 |
| 存疑（uncertain） | 待核实 | 尺寸状态 |
| 事实（fact） | 尺寸与记录；逐构件时写"构件尺寸" | 实测基准表标题"尺寸与记录" |
| 几何、几何版本、几何事实 | 模型、模型版本、构件尺寸 | |
| 构件对象、实体、界面（interface） | 构件、构件、构件间连接 | |
| 稳定键（stableKey） | 编号 | 只在构件详情里显示，正文不出现 |
| 规则层、规则核对、自动核对 | 自动核对 | 不写"规则层"，不写"规则确定项自动执行" |
| 示例资料（producer demo） | 演示数据 | 来源标记 |
| 成组图纸、成果（artifact） | 图纸、成果 | 成果列表主行写中文成果名（成果种类加图幅号），文件名降为次行 |
| 检查记录、检查运行 | 检查结果 | |
| 复核、签发 | 复核、签发 | 保留，是专业流程用词 |
| 资格（检查与资格、检查结果与资格） | 签发（检查与签发、检查结果） | 资格对从业者是人员资质的意思；成果层面说成果等级、能否正式交付（2026-08-24） |
| 词表（vocabulary） | 不出现 | 构件行直接写类型名，如"前檐柱 1 · 柱"，不写"词表 柱" |
| 代理版本、生成新代理版本 | 模型版本、重新生成模型 | 三维模型屏按钮（2026-08-24） |
| 哈希、服务端 | 不出现 | 检查项说明写成"与生成记录逐一对上，出图后没有被改动过"一类可核对的句子 |
| 非任务字段、形制参数层、转写尺寸链 | 固定设置、形制做法、从资料转写尺寸 | 实测基准右卡与操作按钮（2026-08-24） |
| 算不出、这次算不全 | 暂无法计算、影响没有算全 | 费用合计与修改历史的提示（2026-08-24） |
| 项目级页面 | 不出现 | 用户看到的是顶栏上的三个入口 |

**说明文字的规则。** 每屏页头一句，说这一步核对什么或做什么，不解释内部规则；卡片内说明不超过一句；空状态写下一步动作。

