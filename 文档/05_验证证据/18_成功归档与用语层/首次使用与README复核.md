# 首次使用与 README 复核

本轮复核确认，线上展示已能让第一次使用的测绘与文保成果生产人员理解产品用途，并沿高都项目完成从资料到归档的八阶段体验；展示数据与真实工程责任边界已在关键页面明确分开。

日期：2026-08-25。线上地址：<https://heritage.jiajiajiang.com/>。验证版本：`a2fe50c`，分支 `codex/professional-workbench-rebuild`。

## 一、核验依据

本轮以核心 PRD 的目标用户、产品目标、当前范围和非目标为判断基准，不以页面是否能打开代替产品验收。

| 判断项 | 依据 | 首次用户应得到的认识 |
|---|---|---|
| 目标用户 | 核心 PRD 2.1 | 产品服务于古建测绘、文保勘察和成果生产人员 |
| 产品目标 | 核心 PRD 2.5 | 一栋建筑形成一份连续档案，同一份数据驱动模型、图纸、检查和归档 |
| 责任关系 | 核心 PRD 3.4、4.5 | AI 提供理解与候选，程序生成确定性成果，专业人员确认和签发 |
| 当前范围 | 核心 PRD 3.1 | 单栋建筑、本机单用户、完整成果生产链路 |
| 非目标 | 核心 PRD 3.2 | 不代替现场测量、专业判断、法定签发或多人项目平台 |

## 二、验证方法

本轮同时使用自动巡检、真实线上操作和独立首次用户复查，避免只在开发环境或已有本机数据的浏览器中检查。

1. 使用全新 Chrome 用户目录访问线上站点，等待两个展示项目首次装载。
2. 从高都项目的建立任务开始，依次进入整理资料、核对实测、核对构件、记录现状、生成图纸、检查签发和交付归档。
3. 实际打开照片、三维模型、DXF 和 PDF，并翻到 PDF 第二页。
4. 先被动浏览，确认不会自动调用模型；再主动获取建议、提问通面阔，并查看 token 与费用变化。
5. 抽查清式大木构造样板的结构化资料、完成状态和问题数量。
6. 由独立产品设计验收角色使用另一份全新浏览器状态复查项目首页，不读取实现代码。该浏览器连接在进入项目后中断，因此项目内部结论以全新 Chrome 自动巡检和用户可见 Chrome 的逐项操作为准，不把本次独立复查表述为完整项目验收。

## 三、首次使用结论

第一次使用者现在可以从项目列表直接理解产品服务对象、主要输入、处理过程和结果，不必先阅读 PRD 才能开始。

| 用户问题 | 页面给出的答案 | 结果 |
|---|---|---|
| 这是给谁用的 | 项目列表说明面向古建测绘与文保成果生产人员 | 通过 |
| 产品处理什么 | 资料、实测、模型、图纸和归档成果处于同一条可追溯流程 | 通过 |
| 从哪里开始 | 高都项目卡提供进入任务，进入后默认停在建立任务 | 通过 |
| 下一步做什么 | 左侧八阶段与页面顶部下一步按钮共同提示 | 通过 |
| 是否为真实工程数据 | 项目列表、任务卡、检查签发和归档页均说明展示数据边界 | 通过 |
| 谁承担专业责任 | 本机不能签发；随包记录明确为流程演示记录，不构成真实工程签发 | 通过 |
| 能得到什么结果 | 可查看三维模型、SVG、DXF、PDF、检查记录和成果包 | 通过 |

首次装载增加了清单先显示、分包载入、单包重试、缺失项目自动补载和非破坏性继续载入入口。短暂下载失败时保留已成功装载的项目，只补齐缺少的项目，不再要求用户清空本机数据。

全新 Chrome 实测中，两个项目的名称、规模和数据边界在 5.2 秒出现，高都项目在 55.4 秒可进入，两个完整项目在 127.4 秒载入完毕。首次项目包约 42 MB，当前体验已经从全量等待改为先理解范围、再分步进入，但首次完整载入仍偏慢。后续应拆分模型与图纸资源，并使用对象存储或 CDN 按需加载。

## 四、PRD 一致性

展示项目能够表达 PRD 的核心价值，但不会把当前验证范围包装成已经具备生产系统责任。

| PRD 主张 | 可见体验 | 结果 |
|---|---|---|
| 一栋建筑一份档案 | 同一项目贯穿任务、资料、尺寸、构件、模型、图纸与归档 | 通过 |
| 同一数据驱动多种成果 | 9600 mm 实测记录、三维模型、图纸与成果清单连续出现 | 通过 |
| 来源可追溯 | 尺寸显示资料、测量方法和确认状态；构件显示具体资料名称 | 通过 |
| AI、程序与人工分工 | 助手按项目记录回答；程序生成模型和图纸；签发按钮在本机禁用 | 通过 |
| 文件直接查看 | 图片、SVG、DXF 与 PDF 均在页内查看，PDF 支持翻页 | 通过 |
| 修改可追踪 | 修改历史显示操作人、动作、写入数量和下游影响 | 通过 |
| 不代替真实工程责任 | 展示归档明确不可用于工程交付，不显示使用限制为无 | 通过 |

## 五、AI 与用量核验

AI 功能已证明是受用户控制的真实模型调用，不是固定文案或被动页面请求。

- 被动浏览八阶段时，建议调用数保持为 0。
- 点击获取本步建议后，建议次数增加 1。
- 提问项目通面阔后，助手根据项目尺寸记录回答 9600 mm，并说明依据。
- AI 调用与用量页显示模型 `kimi-k2.6`、真实调用次数、token 用量与估算费用。
- 页面说明服务器当日汇总和项目内运行明细的统计范围，费用按公开单价估算。

当前用量页是服务器当日汇总，不是访问者会话账单。该限制已在页面说明，后续若进入多人生产环境，应改为按组织、项目和调用主体分账。

## 六、README 与截图

README 已按专业开源产品的阅读顺序重组，先说明定位、在线体验和主要结果，再提供首次路径、特色能力、边界、架构、运行与文档入口。

参考样本均为官方 GitHub 仓库：[Dify](https://github.com/langgenius/dify)、[AppFlowy](https://github.com/AppFlowy-IO/AppFlowy)、[Plane](https://github.com/makeplane/plane)、[Immich](https://github.com/immich-app/immich)。可迁移的共同做法是把一句话定位、在线入口和结果截图前置，把安装、架构、范围、安全与许可放在后续章节。

最终截图统一使用英文序号文件名，文件名描述功能，不使用页面内部编号。

| 文件 | 证明的产品能力 |
|---|---|
| `01-project-overview.png` | 目标用户、产品价值、两个展示项目与数据边界 |
| `02-survey-data-traceability.png` | 实测记录、照片、确认状态与形制基准 |
| `03-traceable-3d-model.png` | 构件化三维模型与对象树 |
| `04-in-browser-dxf-viewer.png` | DXF 页内查看与图元统计 |
| `05-review-and-signoff.png` | 自动检查、流程演示签发与真实责任边界 |
| `06-deliverable-archive.png` | 多格式成果、来源说明和展示归档 |
| `07-grounded-ai-assistant.png` | 助手按项目记录回答 9600 mm |
| `08-ai-token-usage-and-cost.png` | 真实调用、token 与估算费用 |
| `09-audit-history-and-impact.png` | 修改历史与下游影响范围 |

## 七、当前边界

当前版本可以用于完整产品展示和单机流程验证，但不能作为真实工程生产系统对外承诺。

- 展示实测值用于产品体验，不替代现场实测。
- 流程演示签发记录只说明展示链路完成，不构成真实工程签发或交付资格。
- 浏览器本机项目库不支持多人实时协作、组织权限或服务端统一档案管理。
- 生产环境需要补充身份、权限、服务端项目库、数字签名和正式责任流程。
- 当前展示包首次完整载入约 42 MB，弱网环境下仍可能等待较长时间。

最终全新浏览器巡检覆盖两个项目共 26 个页面，页面异常 0、空页面 0、控制台错误 0。独立角色完成了项目首页的首次理解复查；项目内部的最终验收由全新 Chrome 巡检和用户可见 Chrome 操作共同完成。
