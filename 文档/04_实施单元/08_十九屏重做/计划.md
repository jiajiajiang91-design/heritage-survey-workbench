# 十九屏重做 · 计划

按组执行。每组做完先跑本组验证，通过再进下一组，不攒到最后一起验。

## 一、目标

工作台的每一屏与原型 v4 同一形态，显示的全部是项目的真实数据；旧渲染层退场，`App.tsx` 只剩组合。

## 二、任务组

### 第零组　基线与单元文件

步骤：

1. 提交 8 月 22 日未提交的单元 05、06、07 工作作为基线，不推远端。
2. 写本单元三件套；路线图把单元 08 改为十九屏重做，新增单元 09 第三代动作四项；技术债表追加原型缺陷与姓名字段两条。
3. `.claude/context/pm-context.md` 与 `文档/00_索引.md` 的 159 个变量改为 166。
4. 字体落地：原计划复制仓库内 16.9 MB 的 `NotoSansSC[wght].ttf`，改为加 `@fontsource-variable/noto-sans-sc` 与 `@fontsource-variable/geist` 两个依赖（OFL-1.1，均 5.3.0）。字体文件随依赖本地加载不走外网，中文字体按 unicode-range 分 101 片按需取，不用整包 17 MB；仓库不增加体积。`main.tsx` 先导入两份字体样式再导入令牌，`--font-family-*` 指向带 Variable 后缀的族名。

验证：

- `node .claude/skills/quality-check/scripts/run-all.mjs` 五项通过
- `git status` 只剩字体与本单元文件

### 第一组　地基

步骤：

5. 在工作台源码目录新建 `view-registry.ts`（注册表与 `deriveStageStates`）与 `labels.ts`（十二张标签表），`App.tsx` 再导出；`stage-map.test.ts`、`label-coverage.test.ts` 改导入；给 `ISSUE_TYPE_LABELS`、`DRAWING_KIND_LABELS` 补与 schema 的对账。
6. 新建 `workbench/` 子目录：`useNotices`、`useProjectSession`、`useWorkspaceNav`、`useJobs`、`useEvidencePane`、`useRecordWrites`、`useAssistantBridge` 与门面 `useWorkbench`，另加 `useGeometryBlob`、`useDrawingPreviews`、`download.ts`。纯搬迁，`App.tsx` 仍渲染旧视图。依赖 DOM 的 handler 按规格第四节处理。`useJobs` 在 `useAssistantBridge` 之前调用。
7. `visual-spec.test.ts` 扫描范围改为 `src/**/*.css`（除 `tokens.css`），字面 px 按文件与缺口清单逐行对账；焦点环断言改查组件样式。`字面值缺口清单.md` 改为按文件分行。
8. 重写 `components.css` 与 `ui/`：Button、Tag、SourceTag、Card、Metric、Table、Field、Tabs、EmptyState、Dialog、SplitPane。取值按视觉规范第 5 节与 Figma `02 组件` 页。基础重置与焦点环从 `styles.css` 搬入。
9. 在 `workbench.ts` 加 `listProjectCards()`。
10. 加临时测试：`shell/`、`screens/` 里不得出现旧 `styles.css` 的类名。

验证：

- `pnpm --filter @gujian/workbench test` 全绿，`App.test.tsx` 未改
- 浏览器里基础组件与 Figma `02 组件` 页逐项核对字号、行高、圆角、颜色
- `document.fonts.check` 两款字体为真

### 第二组　外壳

步骤：

11. `shell/AppShell`：三栏网格与三档断点；旧 `styles.css` 的外壳段与媒体查询整段删除，1100 断点并入 1279。
12. `shell/Topbar`、`shell/StageRail`、`shell/TabRow`、`shell/ProjectPageFrame`。
13. `shell/EvidencePane` 与 `ui/SplitPane` 拆开：前者管资料切换、预览、框选、下载，后者只管布局与拖动。
14. `shell/AssistantPanel`：`ChatPanel` 原样嵌入，`assistant-*` 类补样式，选区芯片与清除接 `useEvidencePane`。
15. `App.tsx` 外壳换新，旧视图渲染块暂挂在新外壳内。

验证：

- `App.test.tsx`、`stage-map.test.ts` 通过（重复文本的查询改为限定容器）
- 1440×900 与 Figma W01 截图逐区对照三栏宽、顶栏、左栏行高、助手栏
- 1280、1200、1000 三档读 `grid-template-columns`，无横向滚动

### 第三组　十一个工作区视图

步骤：

16. 按 W01 → W02 → W03 → W04（含 W04A 形式）→ W06 → W05 → W07 → W08 → 成组图纸 → W09（含 W09A）→ W10 的顺序，每屏：加载 `figma:figma-design-to-code` 技能后 `get_design_context` 取节点；写 `screens/X.tsx` 与 `X.css`；同一提交删旧渲染块与旧类，更新缺口清单该文件行；三个演示项目逐个打开核对。
17. 任务确认表单迁入任务卡未确认状态，问题队列改为显示引导项；文档尺寸链核对表单迁入实测基准；`App.test.tsx` 改为在建立任务阶段确认任务，再进记录现状与问题队列断言引导项消失。

验证：

- 每屏替换后 `pnpm --filter @gujian/workbench test` 全绿
- 每屏 1440×900 截图与 Figma 截图并排存入 `../../05_验证证据/17_十九屏重做/`
- `javascript_tool` 抽查标题 22/30、正文 13/20、标签 12/16、圆角 8、描边 1px

### 第四组　项目级页面与弹层

步骤：

18. P01 项目列表（指标卡、项目卡网格、新建卡、搜索、导入、清空），数据来自 `listProjectCards()`。
19. P02 模型运行与用量、P03 修改历史（单栏，影响行同一行）。
20. B01 新建任务、B02 资料缺失、B03 识别未运行、B04 服务恢复。
21. 新建或导入后进入建立任务阶段；`App.test.tsx` 同步。
22. 删除 `App.tsx` 剩余旧渲染与 `styles.css` 旧类，`App.tsx` 收为组合根。

验证：

- `App.test.tsx` 完整流程通过
- P01 三张指标卡与项目卡计数与 `apps/workbench/public/demo/manifest.json` 一致
- B01 新建后进入任务卡

### 第五组　收口

步骤：

23. 缺口清单终态：旧文件行删除，新文件只剩媒体查询；删临时测试。
24. 文档同步：视觉规范 5.1 与 6.3、界面文档第三节页签行、偏离清单追加实现侧偏离、验收逐条填结果、路线图状态、复盘五问。
25. `run-all.mjs --full` 八项全过后提交，不推远端。

验证：

- `node .claude/skills/quality-check/scripts/run-all.mjs --full` 八项全过
- 验收.md 九条逐条填结果，无未过

## 三、风险

| 风险 | 触发条件 | 处置 |
|---|---|---|
| 纯搬迁 hook 改了行为 | `App.test.tsx` 在第一组失败 | 第一组不允许改测试，失败即回查搬迁 |
| 旧类名渗进新页面 | 新页面复用了 `styles.css` 的类或被元素级选择器命中 | 新前缀 `ws-`、`sc-`；临时测试锁住；基础重置先搬进组件样式 |
| 字面 px 对账在迁移中反复失败 | 每删一屏旧类、每写一屏新样式都改变计数 | 第一组先改成按文件对账，旧文件只减不增 |
| Figma 连接中途失效 | `get_design_context` 报未授权 | 停下报 JIAJIA 重新授权，期间做不依赖 Figma 的组 |
| 千条构件列表卡顿 | t0b 1258 个构件一次渲染 | 按类型分组加分页，先做分页，虚拟滚动视实测再定 |
| 资产 URL 泄漏 | 三维模型与图纸预览在页面间切换 | 共用 hook 按 id 串为依赖，卸载时 revoke |

## 四、完成定义

- 验收.md 第一节九条全部通过
- 跑 `node .claude/skills/quality-check/scripts/run-all.mjs --full`，必须修复项为 0
