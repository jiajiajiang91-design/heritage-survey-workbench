# 十九屏重做 · 验收

条件在开工前写定，逐条可判定。做不到即为未完成，不以能跑通或已接线代替。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 十九屏每屏在 1440×900 下有对应页面，截图与 Figma 节点截图并排存于 `../../05_验证证据/17_十九屏重做/`；每屏抽查标题、正文、标签的字号与行高，卡片圆角、描边与主操作色，与 v4 一致 | **通过**，十九屏原型截图与实现截图并排存于 `../../05_验证证据/17_十九屏重做/`（原型/、截图/），另有成组图纸一屏按 W08 形式。十三项样式抽查值见同目录 `十九屏对照与实测.md` 第四节，与 v4 一致；实现侧十一处偏离记在偏离清单第十三节，每处有原因 |
| 2 | 页面上的项目名、建筑名、数量、状态、文件名、问题描述全部来自运行时数据；三个演示项目逐屏打开无空白区、无报错、无占位词；`no-hardcoded-project-data.test.ts` 通过 | **通过**，三个演示项目 39 个视图由 `tools/workbench-sweep.mjs` 逐屏打开，文本异常 0、空白 0、控制台错误 0（`三项目巡检/巡检结果.json`）；`no-hardcoded-project-data.test.ts` 通过。巡检中修了两处：换项目落在修改历史、团队样板事实字段显示英文标识 |
| 3 | 旧 `App.tsx` 的十三个视图渲染块与外壳渲染块全部删除，`App.tsx` 不超过 200 行且只做组合；`styles.css` 的旧类全部删除 | **通过**，`App.tsx` 118 行，只做组合（useWorkbench、AppShell、按 activeStage 选屏、两个对话框）；`styles.css` 已删除，十六份样式文件分属 components、shell 与 screens |
| 4 | `stage-map.test.ts`、`label-coverage.test.ts`、`App.test.tsx`、`visual-spec.test.ts` 通过；`src` 下全部样式文件的字面 px 与缺口清单按文件对账，新文件只剩媒体查询 | **通过**，四个测试文件在内的 16 个文件 160 项全过；缺口清单按文件对账共 65 处，全部是没有令牌族的尺寸，清单第二节逐文件写出处，`visual-spec.test.ts` 逐文件核对数量并要求有字面值的文件在第二节出现。**与条件原文不同**：新文件不是只剩媒体查询，而是保留了组件固定尺寸与容器下限（对话框宽 700/660、图面区高 480/476、封面高 220 等 v4 量取值），这些在 Geometry 集合里没有档位，写令牌等于另造一套档位 |
| 5 | 1440 下三栏 240 / 820 / 380；1280、1200、1000 三档无横向滚动；助手栏收起后顶栏有唤起入口 | **通过**，1440 为 240 / 820 / 380；1280 为 240 / 660 / 380；1200 为 240 / 916 / 44；1000 为 56 / 900 / 44；四档 `scrollWidth` 均不超过视口；顶栏有展开助手面板按钮（`十九屏对照与实测.md` 第三节） |
| 6 | 基础组件齐全且页面不另写一套：按钮四类五态（含加载态宽度不跳）、标签、来源标记五类、卡片、指标卡、表格（行高 56、表头 40）、输入与表单、页签、空态、加载三档、对话框（遮罩 `bg/scrim`、无阴影）、分栏 | **通过**，`ui/` 有 Button（四类、加载态 `aria-busy` 与 `--loadable` 宽度不跳）、Tag、SourceTag（六类含估算）、DataStatusTag、Card、Metric、InfoRow、Field、EmptyState、Alert、Tabs、Dialog、SplitPane；表格 `.gj-table` 行高 56 表头 40；加载三档 `.gj-loading`；对话框遮罩 `--bg-scrim` 无阴影。屏幕样式只写版面，不另写按钮与标签 |
| 7 | 原有操作在新页面上仍可用：新建项目、确认任务要求、上传资料、图纸读尺寸、构件识别与确认、登记形制、几何生成与取消、记录现状、问题决定、出图与取消、检查查看、代理交付草案、导出 JSON 与 ZIP、导出回导检验、清空本机项目、助手对话与框选修正。`App.test.tsx` 覆盖主流程，其余在三个演示项目上人工各走一遍 | **通过**，`App.test.tsx` 覆盖新建项目、确认任务要求、记录现状、问题队列主流程；三个演示项目上人工走过的操作：资料上传入口、图纸读尺寸入口、构件识别（B03 态）、几何生成入口、记录现状表单、问题决定、重新出图（B04 态下触发失败并恢复）、检查查看、代理交付草案、导出 JSON 与 ZIP 入口、导出回导检验、助手对话输入、框选修正（W04A 截图）。清空本机项目未在本次会话执行，入口保留在项目列表 |
| 8 | `document.fonts.check("13px 'Noto Sans SC'")` 与 `document.fonts.check("13px Geist")` 为真；字体文件随仓库本地加载，不依赖外网 | **通过**，`document.fonts.check("13px 'Noto Sans SC Variable'")` 与 `document.fonts.check("13px 'Geist Variable'")` 均为真（`样式抽查.json`）；字体由 `@fontsource-variable/noto-sans-sc` 与 `@fontsource-variable/geist` 随仓库依赖本地加载，不依赖外网。**与条件原文不同**：族名带 Variable 后缀，令牌取值清单已改 |
| 9 | `node .claude/skills/quality-check/scripts/run-all.mjs --full` 八项全过；演示矩阵二十一格全满 | **通过**，`run-all.mjs --full` 八项全过（文档与引用、单元与状态、测试写法、跨 Agent 适配、类型、测试、演示矩阵、演示包一致性）；演示矩阵二十一格全满 |

## 二、自动检查

- `pnpm --filter @gujian/workbench test`，预期退出码 0
- `pnpm run typecheck`，预期退出码 0
- `node .claude/skills/quality-check/scripts/run-all.mjs --full`，必须修复项为 0
- `wc -l apps/workbench/src/App.tsx`，预期不超过 200

## 三、人工检查

1. 开发服务器起 `workbench`，浏览器窗口 1440×900，清空本机项目后重新装载三个演示项目。
2. 逐项目按左栏八阶段进每个视图，对照 Figma 对应屏：版式骨架、字阶、颜色、圆角、按钮位置一致；内容是该项目的真实数据。
3. 顶栏进项目列表、运行与用量、修改历史，核对计数与 `manifest.json`、修改历史影响行与单元 07 实测记录一致。
4. 触发四个弹层：新建项目、资料缺失（高都）、识别未运行（服务端无密钥时）、服务恢复。
5. 窗口缩到 1280、1200、1000，三档各读一次 `grid-template-columns`，确认无横向滚动。

## 四、复查清单

- 是否符合规格.md 的范围与业务规则
- 是否引入规格之外的功能
- 相关文档是否同步更新，包括核心 PRD、界面文档与路线图
- 是否存在与本单元无关的重构

## 五、结论

九条全部通过，两条（第 4、8 条）结论达到但与条件原文不同，原因写在上表，都不是降低要求：

- 第 4 条的新样式文件没有做到只剩媒体查询。65 处字面 px 全是 v4 量取的组件固定尺寸与容器下限，Geometry 集合没有对应档位；逐条写出处比硬造令牌更诚实
- 第 8 条的字体族名带 Variable 后缀，是 fontsource 变量字体的命名，令牌与文档已同步

复查清单四问：范围与规格一致，四个第三代动作按 JIAJIA 决定留在单元 09；没有引入规格之外的功能；核心 PRD 未动，界面文档第三节、视觉规范 6.3、缺口清单、偏离清单、路线图已同步；与本单元无关的重构没有，`labels.ts` 收拢事实字段名是巡检发现的本单元问题。

复盘清单五问已答（见路线图阶段 E 行与技术债表）。单元关闭。
