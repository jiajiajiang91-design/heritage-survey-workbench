# 公开仓库发布 · 验收

条件逐条可判定。做不到即为未完成。

## 一、写定时点的说明

本单元的扫描（12.3 第 3 步）在验收条件写定之前已执行，因为委托里把跑扫描排在建单元之前。扫描属只读操作，不改远端也不改工作区。

两个对外动作（第 1 步建备份、第 4 步替换 `main`）的条件在执行前写定，符合验收条件开工前写定的口径。

## 二、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 扫描报告产出，逐项覆盖密钥、用户名、绝对路径、私有资料、许可、外部 DWG、大对象、过期资格表述八项 | 通过 |
| 2 | 扫描报告中的必须处理项全部处置完毕，每项写明处置方式 | 通过：四项已处置，一项撤回为误判，见报告第六节 |
| 3 | 体量取舍已定，`12.3` 第 2 步的目标与实际一致，不留一个达不到又不改的指标 | 通过：目标改为 100 MB，实际 93.6 MB |
| 4 | 远端存在 `archive/portfolio-v0.0` 分支与 `portfolio/v0.0` 标签，两者精确指向 `ac6abbc` | 通过 |
| 5 | 备份建立在任何其他远端写操作之前 | 通过 |
| 6 | `main` 的内容与本地工作区一致，且不含实施分支的提交历史 | 通过：801 文件，1 个提交 |
| 7 | 发布后的 `main` 上不存在扫描第二节列出的任何一项 | 通过（第一次发布后复扫查出六处遗漏，已清并重发，见报告第七节） |
| 8 | README 写明开发周期与关键节点，替代提交历史承担的时间线说明 | 通过 |
| 9 | 从公开仓库全新克隆后，`corepack pnpm install && corepack pnpm check` 退出码 0 | 通过（改为三步，见下） |
| 10 | 回退演练：确认从 `archive/portfolio-v0.0` 能恢复到 `ac6abbc` 的内容 | 通过：检出得 77 个文件 |

## 三、判定说明

第 5 条不能事后补证。备份必须先于替换，顺序错了即便结果看起来一样，中间那段时间是没有回退点的。

第 7 条要在发布后的远端上重新验，不能用本地扫描结果代替：替换过程本身可能带入未跟踪或新生成的文件。

第 9 条是发布是否可用的唯一硬判据。仓库能打开不等于别人能跑起来。

这一条第一次验时退出码 1：`test:legacy` 跑的旧原型不在 pnpm workspace 里，根目录安装装不到它的依赖，报的是 ERR_MODULE_NOT_FOUND，看不出该做什么。已加 `install:legacy` 脚本走 npm ci 装它自己锁文件里的版本，`test:legacy` 改为先确认依赖再跑、缺了就说该跑哪条命令。README 的跑起来一节相应改为两步安装加一步检查，条件文字里那两条命令随之变三条。

## 四、证据

扫描报告：`../../05_验证证据/13_发布前扫描/发布前扫描报告.md`。

发布后的克隆验证记录写入同目录。

## 四、复查清单

**是否符合规格.md 的范围与业务规则。** 符合。六步按 12.3 原文顺序执行，替换而非整合，备份先于替换，两个对外动作各自单独确认。

**是否引入规格之外的功能。** 引入了一处，写在这里不掩盖：为让全新克隆按 README 跑通，加了 `install:legacy` 脚本并把 `test:legacy` 改为先确认依赖再跑。这不在规格范围内，但它是验收第 9 条不通过时暴露的真实缺陷，不修则第 9 条无法通过。除此之外没有其他功能改动。

**相关文档是否同步更新。** 已更新：技术架构 12.3 第 2 步的体量目标由 50 MB 改为 100 MB 并写明理由；路线图第一、三、四、六节；新增根 README 与 LICENSE；Dai Loy HABS 目录 README 第 7 节的过期状态；四份辅导笔记与两份归档文档的绝对路径。核心 PRD 与界面文档未涉及，本单元不改产品功能。

**是否存在与本单元无关的重构。** 没有。改动集中在发布准备与上手路径，未动领域、应用、几何、制图任何一层。

## 五、结论

十条全部通过。

远端当前状态：`main` 指向 8e00796，801 个文件，1 个提交；`archive/portfolio-v0.0` 与 `portfolio/v0.0` 指向 ac6abbc，77 个文件，作为回退点。

过程中有一件事要记住：第一次替换 `main` 之后复扫又查出六处本机绝对路径，发布前的扫描全部漏掉，已清并重新发布。经过与教训见扫描报告第七节。发布前扫描不要只跑一遍关键词。

本单元已关闭，后续发布按 `pm-context` 的确认规则执行。
