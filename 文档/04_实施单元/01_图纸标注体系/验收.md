# 图纸标注体系 · 验收

条件在开工前写定，逐条可判定。做不到即为未完成，不以能跑通代替。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 标注条数随建筑规模变化，不再是每视图固定条数 | 通过。三个项目分别为 143、144、62 条 |
| 2 | 六类标注全部出现在产物中，每条带出处 | 通过。团队构造样板出详图索引 2 条（横剖面指向 D-02-3，详图回引 D-02-1），另两个项目无详图视图故为 0 |
| 3 | 轴线依据按项目区分并写明，无出处的构件位置不参与定轴 | 通过。柱心线、照片估算柱心线、图上量取墙心线三类分别标注 |
| 4 | 标高取值优先用项目写明值，与几何值的差异有记录 | 通过。Dai Loy 屋脊采用写明值 5943.6 mm，与几何值差 56 mm 已记录 |
| 5 | 被丢弃的标注条数进入视图产物，不静默截断 | 通过。三类丢弃分别计数并写入产物 |
| 6 | 标注由 IR 单一来源下发，四个渲染器不各自推算 | 通过。提交 9b5e347 |
| 7 | 数据冲突不由制图侧选值 | 通过。高都檐高脊高冲突并列记入 `levelConflicts` |
| 8 | 三个演示项目重出图纸，二十一格仍全满 | 通过。全量重建后 `node tools/check-demo-matrix.mjs` 三个项目七格全满 |

## 二、未达成项的处置

**详图索引已补。** 初次判定写的是"三个演示项目均为 0 条，能力已实现但无对应关系"，核对后不成立：团队构造样板确有一个详图视图，是匹配规则写错了。原规则要求详图与母图的剖切偏移完全相等，而檐下承托详图取在横剖面的对称位置（+2400 对 −2400），一条也认不上。改为按剖切面法向认亲、偏移就近匹配后，正向索引与回引都出来了。

另修一处：详图视图的策略行漏了 detailIndex，导致详图上没有回引。质量基准 4.4 写的是"详图上回引它取自哪张总图"，缺回引则拿到单张详图的人无法知道它画的是哪个部位。

**北向符号未出。** 属规格第四节明确不做的范围，不计入未达成。三个演示项目的资料都没有给出正北方向，补的前提是资料里有指北依据，不是画一个箭头。

## 三、证据

实施记录与量化对照见 [标注体系补齐前后对照](../../05_验证证据/12_图纸差距与一致性核对/标注体系补齐前后对照.md)。

## 四、复查清单

- 是否符合规格.md 的范围与业务规则：符合。六类标注按规格落地，轴线依据按有无柱网区分
- 是否引入规格之外的功能：无。北向符号属规格明确不做的范围，未擅自补
- 相关文档是否同步更新：质量基准 4.4 图面细节层级、路线图两处已同步
- 是否存在与本单元无关的重构：无。标注改由 IR 单一来源下发是规格内的必要前置

## 五、结论

八条全部通过，本单元已关闭。
