# 00 · 路线图

阶段 D 公开发布已经完成，当前处于阶段 E。2026-08-21 阶段 E 的范围由第三代动作四项改为界面重做。2026-08-22 单元 05 完成，重做的形式基准从 v3 改为 v4：v4 继承 v3 的全部视觉参数，内容换成代码里的真实数据。2026-08-22 晚 JIAJIA 定：单元 08 只做前端页面层重建，四个第三代动作拆成单元 09 在新页面上接；v4 只给形式，内容来自运行时真实数据，不要求原型与网页逐字一致，新建页面层不在旧 `App.tsx` 上改。

| 阶段 | 目标 | 范围 | 依赖 | 验收 | 状态 |
|---|---|---|---|---|---|
| A 资产保全 | 固定可恢复的工程基线 | T0a、T0b，资产分类、迁移、CAD 可行性与恢复 | 现有仓库资产与验证样本 | 迁移、冷安装、恢复和 CAD 验证证据齐全 | 已完成 |
| B 领域与成果管线 | 跑通单栋建筑的工程闭环 | T1 至 T13、14 个助手动作、R1 至 R4 | 阶段 A、核心 PRD、技术架构 | 里程碑一至三、动作层、规则层与词表验证通过 | 已完成 |
| C 展示成果 | 形成可公开展示的完整链路 | 三个演示项目二十一格、01 图纸标注体系、02 框选修正接入 | 阶段 B、演示项目定义、项目照片授权 | 二十一格全满；单元 01 为 8/8，单元 02 为 10/10 | 已完成 |
| D 公开发布 | 提供可访问、可安装、可恢复的公开仓库 | 03 公开仓库发布、T15 非破坏发布 | 阶段 C、发布方案与对外动作授权 | 单元 03 为 10/10；公开主分支、历史备份与全新克隆验证完成 | 已完成 |
| E 界面重做与 v1.0 功能补齐 | 界面对齐产品原型 v4，同批补完已有 PRD 依据的第三代动作 | 05 设计参数补全与原型对齐、06 外壳与令牌、07 影响范围能力、08 十九屏重做、09 成功归档与用语层、10 第三代动作四项（F17、F01、F12、F08） | 阶段 D、产品原型 v4、功能条目、依赖与失效规则 | 六个单元各自验收全通过；全量质量检查通过；二十一格保持完整 | 进行中，单元 05、06、07、08、09 已收口，10 待开 |
| F 试点验证 | 验证业务指标与乙方付费假设 | 同任务、同资料、同角色配置的人工与产品对照；H2 付费意愿 | 阶段 E、试点团队、真实资料传输授权、人工基线 | 追溯完整率 100%；重复录入减少 80% 以上；周期缩短 50% 以上；形成 H2 验证记录 | 未开始 |
| G 私有部署与正式交付 | 接入真实身份、权限、签名与正式发布 | PRD v2.0、T14 私有部署责任适配 | 阶段 F 结论、私有部署决策、架构变更确认 | 身份、权限、签名、验签和正式发布状态机通过 | 未开始 |

## 阶段 E 的六个单元

按硬依赖排序，前一个验收通过才进入下一个。

| 单元 | 做什么 | 为什么是这个顺序 |
|---|---|---|
| 05 设计参数补全与原型对齐 | 已完成。取全设计系统 148 个变量，在 Figma 建 v4 十九屏，内容全部换成代码里的真实数据 | 参数不全就动手等于让实现自己发挥。v3 的内容是画原型时编的示意值，与真实项目对不上，直接照它实现会做出界面与数据互相矛盾的产品 |
| 06 外壳与令牌 | 三栏尺寸、八步导航、页签层、项目级页面移到顶栏；令牌换 v2.0；页面样式整体重写；补间距圆角字重的自动校验 | 外壳影响每一屏，先换才不会让内容屏做两遍 |
| 07 影响范围能力 | 已完成。依赖图按记录已有的引用字段推导而非另存，`ImpactService` 算下游闭包，修改历史每条带影响行，`previewImpact` 供单元 08 的动作层调用 | 修改历史每条带影响行、变更任务参数要先展示影响，两处共同前置。原来列的第三处局部退回按检查项退回，单元 05 核实产品里没有这个功能，v4 已改为资格与使用限制，不再作为本单元的驱动 |
| 08 十九屏重做 | 已完成。外壳、十三个基础组件与十九屏对应的页面层按 v4 节点重建，内容绑定运行时真实数据；`App.tsx` 收为 118 行组合根，`styles.css` 删除；十九屏原型与实现截图并排、三档断点、三项目巡检见 `05_验证证据/17_十九屏重做/`；九条验收全过，两条与原文不同的原因记在验收里 | 依赖前三个。单元 06 只换了令牌没重做页面，页面层要整体重建才能与 v4 同形 |
| 09 成功归档与用语层 | 已完成。三个演示包重建为归档完成（资料齐全、尺寸实测、问题为零、复核签发），领域层新增复核签发记录且本机授权拒绝签发；界面用语表进界面文档表 13 并由 `wording.test.ts` 锁住；助手的建议与回答由模型按项目现状生成；图片、PDF（含 JBIG2 扫描件）、SVG、DXF、CSV、JSON 页内查看；中栏整体滚动。九条验收全过。2026-08-23 至 24 两轮独立试用（agent 以新用户身份实测）发现的六组问题已整改并复验：签发后交付判定、档案时间线、项目包携带形制记录、图面标高与图签口径、用语层收尾、助手接地，记录见 `05_验证证据/18_成功归档与用语层/独立试用复查与整改.md` | 复核签发记录只能随包导入，本机仍不能签发，铁律不破 |
| 10 第三代动作四项 | 待开。退回重做、变更任务参数、设置图纸样式、存入构件库在新页面上接，沿用单元 04 的规格与十二条验收。页面层已定：动作入口落在 W01（变更任务参数）、W08（设置图纸样式）、W09（退回重做）、W04（存入构件库），`useRecordWrites` 是动作层的接线处 | 动作层改动不与页面重写交织，界面先成型 |

范围口径：形式（文字样式、颜色、按钮、形状、间距）完全照原型 v4，内容照代码里的真实数据。v4 见 Figma 文件 `JjPzJEu3boS7xFFlERfs3B` 页面 `05 产品原型 v4`。

单元 04 第三代动作四项已搁置，四个动作改由单元 10 实施，其规格与验收条件沿用不重写。

第三代动作的其余三项（转办待办、降级出草图、录入实测记录）缺 PRD 功能编号，先改 PRD 再另排单元，不进阶段 E。

单元 10 有一条约束来自单元 07：变更任务参数这个动作要先展示影响，但任务书与出图要求之间没有记录互相引用，程序算不出改任务要求会作废哪些图纸。`previewImpact` 会返回一条说明而不是一个偏小的清单。单元 10 实现这个动作时，界面要如实显示算不出来，不能拿一个空清单当没有影响。

单元 08 的字阶前置已在 2026-08-22 解除：`Typography` 补了七条，十九屏用到的行高全部有变量，字号只剩加号字符的 30px 不进字阶。

### 重做前查清的现状

逐屏勘察与现状对照记录见 `../05_验证证据/14_界面重做/原型v3勘察记录.md`。三条影响做法的结论：

令牌层干净，页面层另起一套。`tokens.css` 37 个变量与视觉规范 v1.0 逐字一致；`styles.css` 395 行里有 585 处字面 px，间距大量不在规范六档内。因此令牌可整体替换，页面样式要整体重写。

自动校验挡不住尺寸偏离。`visual-spec.test.ts` 只挡字面色值与 12px 字号下限，不查间距、圆角、字重，这是 585 处字面 px 能长期全绿的原因。单元 06 要把这三项加进校验。

基础组件只覆盖三分之一界面。`gj-` 类在 tsx 里出现 91 次，其余走页面自定义样式。不先划清这条边界，换完令牌偏离会重新积累。

## 技术债

单元收口时绕开的问题记在这里，不留在注释或对话里。

| 项 | 内容 | 何时处理 |
|---|---|---|
| ~~视图标签三方不一致~~ | 已结。2026-08-22 单元 06 第四组把代码的 label 统一到表 2，共改五处（另有模型运行与费用改为模型运行与用量）。表 2 末列已删，`stage-map.test.ts` 直接解析表 2 表 3 与代码比对 | 已完成 |
| ~~模型运行与用量的层级~~ | 已结。2026-08-22 三个项目级页面移到顶栏右侧，进入后只出中栏，左栏与助手栏不显示 | 已完成 |
| ~~成组图纸没有对应屏~~ | 已结。2026-08-22 单元 08 按 W08 的形式规则实现了成组图纸视图（`screens/DrawingSet.tsx`），截图存在 `17_十九屏重做/截图/W08b_成组图纸.png`，不补屏 | 已完成 |
| ~~视觉规范 v2.0 未写~~ | 已结。2026-08-22 单元 06 第五组成文，九节，取值来自 Figma v4 的 159 个变量。索引、pm-context、README 三处指向已同步 | 已完成 |
| ~~字阶变量盖不住画面~~ | 已结。2026-08-22 Figma 重新授权后补入七条（`size/20`、`size/22`、`size/28`、`line/18`、`line/26`、`line/30`、`line/38`），令牌总数 159 增至 166。执行前重扫 1339 个文字节点核对，纠正了原记录的两处错，收口见缺口清单第七节 | 已完成 |
| ~~Figma 连接需重新授权~~ | 已结。授权已恢复，卡住的三件事一次做完：字阶七档已补；主操作色三值与品牌方块圆角 7px 经扫描确认 v4 上本来就不存在，是 v3 勘察时的记录登记晚了 | 已完成 |
| 设计系统没约束住原型 | Figma 那一侧字号、行高与圆角基本没绑变量：1339 个文字节点零绑定，468 个 8px 圆角节点只有 19 个绑了 `radius/md`，只有颜色绑得齐。集合目前是写在旁边的说明，改值画面不跟着变。不影响 `tokens.css` 的正确性 | 单元 08 不改 Figma，另排一次性任务 |
| 全量检查偶发未过 | 2026-08-22 的九次 `run-all --full` 里有两次测试这一步退出非零，两次紧接着单独重跑都全绿（419 项），此后连跑三次也全过，没能复现，两次都没抓到失败用例。已知不是断言问题 | 下次再遇到先把整段输出存文件再排查，不要只看结尾几行 |
| 形制推算链与任务书链未留痕 | 两处引用缺失让影响范围算不全：几何对象引用 `archetype:<ruleSetId>:<key>` 合成字符串而非 `ArchetypeSpec` 的记录 id；`ArtifactRequirementMatrix` 只指回几何版本，不指回产生它的任务书。后者直接卡住 F01 的任务要求变更时给出受影响成果清单。单元 07 已把两处显式报出来，没有补链路。2026-08-24 复查时 `ArchetypeSpec` 已随项目包携带（包格式 archetypeSpecs 一节），影响链本身仍未补 | 补法是让记录之间互相引用，牵动几何管线与三个演示包，另排单元 |
| t0b 演示包的几何引用指向包外 | 1258 处 `demo:v3-entity:<uuid>`，是 v3 转换时保留的原始构件 id，包里没有对应记录。这个包从资料出发的链是通的，从构件出发算不出下游 | 重建 t0b 包时把这些引用换成包内记录 id，或明确记为该包的已知限制 |
| v4 的 P01 屏两处布局缺陷 | 顶栏被排在画板底部（其余十八屏在顶部），高都卡片的说明文字溢出到图片区外。单元 08 实现按其余十八屏放顶部、说明放进占位区，不回改 Figma | 下次改原型时顺手修 |
| 操作人没有姓名字段 | 裁决第三节第 8 条定了演示项目补真实姓名字段，领域层 `ResponsibilitySchema` 至今只有 role 与 actorId。单元 08 按退回规则显示角色名 | 要显示人名时另排数据模型改动 |
| 单价会过期 | `apps/workbench/src/model-pricing.ts` 的 kimi-k2.6 单价取自 2026-08-22 的官网定价页，出处与日期记在表里 | 每次涉及费用口径时回官网核一次 |
| 事实字段没有名称字段 | 事实记录只有 `field` 标识，中文名靠界面 `FACT_FIELD_LABELS` 翻译已知词汇；团队样板的 36 个 `DIM-*` 稳定键对不上，按标识显示。领域层给事实加名称字段是数据模型改动 | 与操作人姓名字段一起排数据模型改动 |
| 按下态没有专属色 | v4 只定义了悬停色，按钮按下态暂与悬停同值 `action/primary-hover-bg`。视觉规范 5.1 仍要求按下态 | 下次改 Figma 时补 `action/*-active-bg` 四条 |
| 窄屏三档没有原型 | 1280 以下三档按裁决第一节第 5 条推导实现，v4 一屏都没画；实测记录在 `17_十九屏重做/十九屏对照与实测.md` 第三节 | 要改窄屏形式时先补原型 |
| 构造样板的翻译近似 | 909 条构件级翻译近似说明（瓦件断面展平、局部接触接口未携带）由签发记录接受，几何契约本身没有升级 | 几何契约支持局部接触面语义时重新翻译 |
| 演示包更新要清空重装 | 仓储没有按项目删除，演示包有新版本时只能清空本机全部项目后重装（列表页有提示与确认） | 仓储加按项目删除后改为逐项目替换 |
| 图签签发状态是生成时点 | 图纸在签发前生成，图签只能印"待签发成果·见签发记录"，指向签发记录而不是印签发章。正式环境签发后对图面重签（盖章、日期栏）没有流程 | 阶段 G 私有部署接真实签名时一并做图面重签 |
| 驳回候选的审计动作名 | 驳回识别候选写入修改历史的动作名是"保存识别结果"加"运行规则核对"，看不出是一次驳回；实现复用了两个既有命令 | 给候选处置加专属命令类型时一并改 |
