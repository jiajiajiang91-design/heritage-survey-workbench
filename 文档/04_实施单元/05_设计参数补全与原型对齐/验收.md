# 设计参数补全与原型对齐 · 验收

条件在开工前写定，逐条可判定。做不到即为未完成。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 六类设计参数逐项列出取值，每项标明来源方式（变量／节点属性／截图量取）与来源节点 | 通过。`14_界面重做/设计参数取值表.md`：138 个变量分四个集合逐条列出，另列节点级用法与来源节点 |
| 2 | 取不到的参数逐条列为缺口，写明缺什么、为什么取不到、建议怎么定，不填推测值 | 通过。`设计参数缺口清单.md` 列五处真实缺口。初版误判的 53 处已作废并写明错因 |
| 3 | 主操作黑色的准确色值已确定并定义为令牌 | 通过。`action/primary-bg` = `#0a0a0a` 本就是变量；v4 把此前未走变量的 61 处深色填充全部绑到它 |
| 4 | 来源标记五类的色值与五种形状的画法参数（点径、描边宽、方点边长、斜线角度线宽间距）齐全或列为缺口 | 通过。2026-08-22 补 `source/human` 三值与 Tone=Human 后，来源五类各有专属变量与色调（实测 Info、AI 识别 Ai、自动核对 Rule、人工确认 Human、示例资料 Neutral）。形状参数仍缺，列在缺口清单第 2 条 |
| 5 | 字阶六档齐全，每档含字号、字重、行高三要素 | 通过。按裁决第 3 条不收敛，改为按用途逐项列出。取值表第四节覆盖十九屏全部文字节点、21 种组合，并与 v3 实测比对确认集合完全相同。该表列了用途与出现屏，未逐项标来源方式与来源节点，这一点弱于第 1 条的要求，记在此处 |
| 6 | 勘察记录里的七处疑点整理成问题交 JIAJIA，逐条有裁决结果 | 通过。`缺口与疑点裁决记录.md` 共九条处置，含 JIAJIA 直接裁决四条 |
| 7 | Figma 新建页面 `05 产品原型 v4`，十九屏齐全 | 通过。页面 `66:1559`，十九屏，其中五屏按实现现状改名 |
| 8 | v4 每屏的内容取自当前代码的真实数据，项目名、对象名、构件编号、操作人、文件名、数量、状态文字均可回溯到代码里的出处 | 首轮不通过，复审后通过。2026-08-22 的完整性审计查出 24 条可回溯性问题，含 W02 整屏未更新、W01 把第三方照片写成项目自有、W07 类型标签自造词等，已逐条改完，逐条记在偏离清单第十节 |
| 9 | v4 与 v3 逐屏对照：版式、字号、字重、行高、颜色、按钮样式、圆角、间距无一处改动 | 有条件通过。字阶实测两边各 21 种组合、集合完全相同。信息结构按职责边界改了 14 处，第二轮又改 24 处，全部逐条记在 `v4对v3的偏离清单.md` 第二至五节与第十节 |
| 10 | 内容变长导致排版放不下的地方，按 v3 已有规则处理并记录，未新造样式 | 通过。一律改为自动布局的随内容长高，未新增任何样式或色值。W04A 新增的五类清单全部复用既有令牌 |
| 11 | 参数取值表与缺口清单写入 `../../05_验证证据/14_界面重做/` | 通过。该目录现有五份：勘察记录、取值表、缺口清单、内容对照表、裁决记录，另加偏离清单 |
| 12 | `node .claude/skills/quality-check/scripts/run-all.mjs --full` 必须修复项为 0 | 通过。2026-08-22 修复缺陷并重建三个演示包后复跑，六项检查全通过，演示矩阵三个项目七格全满 |

## 二、判定说明

第 1、2 条是本单元的核心。判定时看的是每一项有没有标来源，以及缺口有没有如实列出来。填了一个没有来源的值，即便它看起来合理，也算未通过。参数不全就动手，等于让实现自己发挥，当前 `styles.css` 里 585 处字面 px 就是这么来的。

第 9 条要逐屏比对，不能只看整体印象。v4 的目的是只换内容，形式改动一处就破坏了它作为实现依据的价值。

第 8 条的判定方式是抽查：任取 v4 上的五个具体数字或名称，都能指出它在代码或演示数据里的出处。指不出来的说明是编的。

第 6 条不能跳过。七处疑点里有几处涉及产品判断（两个新建入口是否同一动作、阶段与页签是否多对多），实现时绕不开，现在不定就是把决定推给实现者。

## 三、复查清单

四条都要回答，不能跳过。

- 是否符合规格.md 的范围与业务规则
- 是否引入规格之外的功能
- 相关文档是否同步更新，包括核心 PRD、界面文档与路线图
- 是否存在与本单元无关的重构

## 四、证据

参数取值表、缺口清单、疑点裁决记录、v4 与 v3 的逐屏对照结果，全部写入 `../../05_验证证据/14_界面重做/`。

## 五、结论

十二条全部通过，第 9 条为有条件通过：视觉参数经实测比对确认，信息结构按职责边界有记录地改动，逐条见偏离清单。

三轮的经过：第一轮十九屏改完就写了结论，判定过早。第二轮的完整性审计查出 24 条实质问题，含 W02 整屏没跟上、W01 把第三方照片写成项目自有并打勾已确认，全部改完。第三轮把原本挂起的三条待定决定掉并执行。教训写在偏离清单第十节：屏改完不等于对，要用与改动无关的方式复核一遍。

原来列为待 JIAJIA 定的三件，2026-08-22 由 project-lead 决定并执行，理由与回退方式记在裁决记录第七节：

一、v3 不分叉，继续与 v4 共用一套组件；v3 页面加了状态说明，写明继承的三处改动与冻结方法。
二、W07 与 B01 的两处一次性字号判为画图手误，归到多数值。
三、来源标记不做形状，口径改为颜色加文字标签；视觉规范与 README 已同步。

设计参数缺口清单的五处缺口全部收口，见该文第五节。

审计中查出三个产品缺陷，均已修：规则核对一律作废未解决问题、项目包不带命令回执、界面说图签日期栏留空而实际印未签发。三者都不属本单元范围，记在对照表第四节。三个演示包已按修复后的代码重建。

本单元可以关闭。下一步是单元 06 外壳与令牌：把 v4 的 148 个变量落成 CSS 令牌，替掉 `styles.css` 里的 585 处字面 px。
