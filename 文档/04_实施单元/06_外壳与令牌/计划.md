# 外壳与令牌 · 计划

按组执行。每组做完先跑本组验证，通过再进下一组，不攒到最后一起验。

## 一、目标

工作台的令牌层与外壳结构与产品原型 v4 一致：样式里不再有来路不明的字面数值，左栏按八阶段导航，项目级页面在顶栏，窄屏有确定的收起顺序。

## 二、任务组

### 第一组　令牌层换成 v4

步骤：

1. 从 Figma 文件 `JjPzJEu3boS7xFFlERfs3B` 导出四个集合的全部变量，含别名指向，写成一份取值清单放进 `../../05_验证证据/15_外壳与令牌/`。已完成
2. 重写 `apps/workbench/src/tokens.css`：变量名由 Figma 变量名转 kebab-case，语义层保留对 Primitives 的指向关系，不把别名摊平成字面值。已完成
3. 在 `visual-spec.test.ts` 增一条：令牌文件里的变量集合与取值清单逐条相等，缺一条或多一条都不通过。已完成，另加一条锁语义层不写字面色值

验证：

- `corepack pnpm --filter @gujian/workbench exec vitest run src/visual-spec.test.ts` 退出码为 0。已通过，11 条
- 取值清单里的每个变量都能在 `tokens.css` 里找到，反向亦然。已通过，双向锁在测试里，加一个来路不明的令牌会失败，已实测

第一组结果：新令牌 148 条落盘，与旧令牌 46 条并存无重名。旧令牌待第二组迁完引用后删除。

### 第二组　字面数值改令牌引用

步骤：

4. 逐条处理 `styles.css` 的 585 处与 `components.css` 的 46 处字面 px。每处先在取值表第三节查该处的原型取值，命中令牌就换成引用。已完成，换掉 337 处
5. 查不到出处的逐条记入缺口清单，不就近取档。缺口清单放进 `../../05_验证证据/15_外壳与令牌/`。已完成，288 处分四类记录
6. 在 `visual-spec.test.ts` 增三条校验：间距只取 Geometry 的十二档、圆角只取六档、字重只取三档。已完成，实际加了六族的引用合法性校验加两条清单对账

验证：

- `visual-spec.test.ts` 的间距、圆角、字重三条通过。已通过，共 19 条
- 两份样式文件里剩余的字面 px 数量与缺口清单条数相等，逐条对得上。已通过，288 对 288，由测试锁住

开工前先做了一次清点，发现两件必须先处理的事，都是 v4 自身的问题：

一是档位表漏了一档。v4 十九屏有 124 处内边距用 10，遍布顶栏、信息行与检查行，而 Geometry 的间距十二档里只有 8 与 12。补 `space/10` 进 Figma 与令牌表，档位由十二档变十三档。不补的话这 124 处在实现里只能写字面值。

二是两处画图手误。圆角 6 共 8 处（表头与树行），同类另有 460 处是 8；间距 2 共 5 处，全在 W04A 的五类清单里，是补那块内容时我自己写的。都归到多数值，记入偏离清单。

第二组结果：337 处走令牌，288 处记入缺口清单。缺口分四类，甲乙丙三类共 161 处属版面几何与效果值，归第三组随外壳重写处理；丁类 127 处是各视图内容样式里脱档的间距，取值来自视觉规范 v1.0 而非 v4，归单元 08 逐屏实现时按 v4 重写。

### 第三组　三栏与窄屏

步骤：

7. 三栏尺寸按取值表改为顶栏 56、左栏 240、右栏 380、中栏最小 640，全部走令牌。已完成，另补 layout 段十条令牌
8. 实现三档断点：不小于 1280 三栏全开；1024 至 1279 收起助手栏并在顶栏出唤起图标；小于 1024 左栏收为图标条。已完成
9. 在 `visual-spec.test.ts` 增一条：三档断点的取值与规格第三节一致。已完成，另加两条：v4 无阴影、来源标记新口径

验证：

- 三档宽度下各截一张图放进证据目录，与 v4 的形态对照无出入。**改为读计算样式**：浏览器面板本次未显示，截不了图；改在开发服务器上逐档读 `grid-template-columns`，比截图更硬。1440 一档实测 240 / 820 / 380，与 v4 画板逐像素一致。记录见 `../../05_验证证据/15_外壳与令牌/三档断点实测.md`
- `visual-spec.test.ts` 退出码为 0。已通过，共 21 条

第三组另做了三件计划外但必要的事：

一是补齐版面令牌。Figma 原来没有 layout 族，三栏尺寸与断点在实现里只能写字面值。补十条进 Geometry，五条量自画板、五条按裁决推导，描述里分别标明。

二是按 v4 删掉实现里的阴影与形状底纹。v4 十九屏一个阴影都没有，改用一像素描边；来源标记的斜线底纹与圆点属已取消的形状口径。两处都由新增测试锁住。

三是修掉两个自己引入的错。最小宽度写成断点值导致小于 1024 时横向滚动，改为按三栏下限算得 740；左栏图标条误用顶栏高度令牌，补 `layout/rail-icon` 专用令牌。

**同时发现第二组的六条档位校验是空转的。** 用模板字符串拼正则时反斜杠被吃掉一层，`var\(` 变成 `var(`，括号被当成分组，永远匹配不到。塞一个不存在的令牌实测确认能通过。已改成写死的正则再按前缀筛，重新实测确认能失败。

### 第四组　八阶段导航与视图改名

步骤：

10. 左栏由十三视图平铺改为八阶段，阶段状态三态：当前、已完成、未到。已完成，第三态改称未开始，理由见下
11. 中栏在阶段含多个视图时出页签行，页签名取界面文档表 2；单视图阶段不出页签行。已完成，核对构件、记录现状、生成图纸三个阶段出页签行，其余五个不出
12. `stages` 的 label 统一到表 2 的四处改名，stage id 保持不动。已完成，实际改了五处：另有模型运行与费用改称模型运行与用量，对齐表 3
13. 项目列表、模型运行与用量、修改历史移到顶栏右侧，进入后不显示左栏与助手栏。已完成
14. 增一条测试：`stages` 的每个 id 都能落到表 2 的某一行，且 label 与表 2 一致。已完成，`stage-map.test.ts` 六条

验证：

- 新增测试退出码为 0。已通过。测试直接读 `03_界面与交互形态.md` 解析表 2 表 3，不是把文档内容抄进测试。把 evidence 的 label 改回项目资料实测确认能失败
- 全量测试通过，助手动作层的用例不受影响（stage id 未改）。已通过，152 条。`App.test.tsx` 的导航改成两层：先点左栏阶段，多视图阶段再点页签

**第三态由未到改成未开始。** 原写法暗示按序推进，实测对不上：高都演示包里记录现状没有数据，而它后面的生成图纸有 12 项产物、检查签发已检查，按原口径要把这三个已经做完的阶段显示成未到。本产品允许资料先到现状后补，判据改为只看该阶段自己有没有数据。规格第 47 行已同步。

顺带补了两处：一是阶段点不能只靠颜色区分，每行加一句读屏可取的文字；二是视图切换收到一个入口 `goToView`，左栏、页签、顶栏、待办和助手动作层原来各写各的。

上一步下一步原来按 `stages` 数组下标走，会从代理交付推到模型运行与用量这个项目级页面上。改为按八阶段展开的十一个工作视图走。

### 第五组　视觉规范 v2.0

步骤：

15. 按 v4 的实际取值写 `../../02_技术/02_界面视觉规范.md` 的 v2.0，保留 v1.0 的第 5 节交互状态与第 7 节可访问性。已完成，九节。第 7 节整节保留，第 5 节的规则保留但三处取值按 v4 实测改了，见验收第 11 条
16. 更新 `文档/00_索引.md`、`.claude/context/pm-context.md`、`README.md` 三处对视觉规范的指向说明。已完成
17. 按复盘清单五问收口。已完成

验证：

- `node .claude/skills/quality-check/scripts/run-all.mjs --full` 必须修复项为 0。已通过，八项全过（本组新增第八项测试写法）

第五组做了两件计划外的事，都是写 v2.0 时查出来的。

**一是字阶变量盖不住画面。** 写第 3 节字体与字阶时，把取值表第三节那 19 项用法的取值与 `Typography` 集合逐个求差集，查出五个字号四个行高用了但没有变量，另有两个字号两个行高有变量但没人用。这是 `space/10` 那类问题，范围更大。判断与逐档处置写进缺口清单第六节，补变量的动作卡在 Figma 授权，记入技术债。第 3 节的表里逐行标了哪些档有变量哪些没有，不含糊过去。

**二是旧令牌一个都没删。** 验收第二节要求用与改动无关的方式复核，复核时发现第一组说的迁完再删从没执行：46 条 v1.0 旧令牌还在 `tokens.css` 里，样式里引用 384 处。其中 `--accent` 是蓝色 `#2563EB`，51 处引用，与刚写完的规范 v2.0 第 2 节主操作色是近黑直接矛盾。

已按用途逐条迁完：383 处换成新令牌，旧令牌段整段删除，`tokens.css` 现在正好 159 条与 Figma 相等。`--accent` 的 51 处不按模式猜，按选择器加属性逐条指定去向，分四类：主操作底、文字按钮、选中与强调文字、聚焦与选中边框。测试新增一条锁住 46 个旧名字不再出现。

迁移中一并按 v4 改了两处：表格选中行的 2px 竖条删掉只留底色；助手输入框此前没有任何样式规则，文字取浏览器默认纯黑，按规范第 5.5 节补齐。两处产生的两个字面值（表格行高 44、多行输入高 46）都记进了缺口清单，没有就近取档。

## 三、风险

| 风险 | 触发条件 | 处置 |
|---|---|---|
| 631 处字面值里大量查不到出处 | 取值表只覆盖 v4 十九屏用到的位置，样式表里有屏上没出现过的状态 | 查不到就记缺口，不就近取档。缺口超过五十条时停下来先回 Figma 补画这些状态，不要带着猜测继续 |
| 改名牵动动作层 | 误改 stage id 而非 label | 第四组的验证专门锁这一点：id 不变、label 与表 2 一致 |
| 令牌改名影响未覆盖的样式 | 旧令牌名在样式里仍有引用 | 第一组先建新令牌不删旧的，第二组改完引用后再删，删除后跑全量测试 |
| 三栏断点无原型可对 | 断点是按裁决推导的值，不是原型取值 | 三档各截图存证，规范 v2.0 里标明是推导值 |

## 四、完成定义

- `tokens.css` 的变量与 Figma 四个集合逐条相等
- 两份样式文件里剩余的字面 px 与缺口清单逐条对得上，没有无出处的数值
- `visual-spec.test.ts` 覆盖色值、字号、间距、圆角、字重、断点六类
- 左栏为八阶段，页签行按阶段出现，视图名与界面文档表 2 一致，stage id 未改
- 三个项目级页面在顶栏，进入后不带左栏与助手栏
- 视觉规范 v2.0 成文，三处指向说明同步
- 跑 `node .claude/skills/quality-check/scripts/run-all.mjs --full`，必须修复项为 0
