# 框选修正接入 · 验收

条件在开工前写定，逐条可判定。做不到即为未完成，不以能跑通或已接线代替。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 在证据区照片上拖出选框后，输入区出现位置引用，可移除 | 通过（无截图，见实测记录第二节） |
| 2 | 框选后输入一句话发送，构件表新增对应行，来源标记为人工确认 | 通过 |
| 3 | 该次修正出现在修改历史中，含理由、操作人、时间 | 通过（2026-08-21 补测，见实测记录第九节） |
| 4 | 五类说明方式各执行一次并达到预期：新增、类别修改、位置调整、删除、标记不可见 | 通过 |
| 5 | 删除类修正进入排除记录，重新识别后该项不再出现 | 通过（2026-08-21 补测，见实测记录第八节） |
| 6 | `client-op-adapter.ts` 中不再存在返回尚未接入的分支 | 通过 |
| 7 | bbox 无法关联构件时界面向用户提问，不静默失败 | 通过 |
| 8 | 代码登记表中 `marquee_correction` 状态为 `executable`，界面文档表 10 同步，`action-catalog.test.ts` 通过 | 通过 |
| 9 | 分发用例集三条框选用例执行完毕，结果回填用例集第 3 节 | 通过：三条全部命中 |
| 10 | `npm run typecheck` 退出码 0，`npm run test` 全绿 | 通过（附注：`App.test.tsx` 一条测试并发跑时偶发超时） |

## 二、三处差异的处置（2026-08-20 已定）

JIAJIA 定为三条全部改实现，文档不降级。判断依据：新增与遮挡标记逐条比对 PRD 附录 A.3 的五条自动执行条件全部满足，属于该删的固定确认；删除与位置调整的成因是构件缺图像位置，补上即可成立。

| 序 | 要求 | 落地 |
|---:|---|---|
| 1 | 新增与遮挡标记直接执行并留痕，不走确认 | 两者都已直接执行。遮挡标记经 `ReviseEntities` 写入构件记录的 `visibility`，原因随记录留存，构件行显示不可见 · 需补拍 |
| 2 | 删除命中构件后进排除记录，重新识别时过滤 | 排除记录已实现并带理由、操作人、时间，构件行显示已排除。重新识别时的过滤有单元测试，未经端到端验证，原因见验收第 5 条 |
| 3 | 位置调整命中构件后重算相邻关系 | 位置本身已真改：采纳后 `imageRegion` 与位置说明同步更新。相邻关系重算不在本单元范围内 |

原以为随构件带图上位置一并成立的两项，实测表明并不成立。类别修改、位置调整、标记不可见此前只写一条事实，构件记录原样不动：采纳把类别改成撑栱之后构件表仍显示待确认，位置调整之后下一次框选命中仍按旧框判。按本条决定改实现，新增 `ReviseEntities` 命令按 id 替换构件记录，事实与记录都写，事实那条承载来源与理由不删。经过见 `../../05_验证证据/10_动作层验证/框选修正端到端实测记录.md` 第四节缺陷 7 至 10。

## 三、判定说明

第 8 条是本单元的防虚报条件。此前该动作在 PRD、界面文档与产品状态入口三处记为已实现，而前端只有桩，原因是文档可以单方面宣称交付。现在实现状态由代码登记表派生并有测试守着，文档与代码不一致会在测试中失败。

第 9 条不可跳过。这三条用例自动作层验收起一直标为待框选界面接入后补测，本单元完成即失去延后理由。

## 四、证据

截图与执行记录写入 `../../05_验证证据/10_动作层验证/`。

第 1、6、8、10 条见 `框选交互实测记录.md`。

第 2 至 5、7、9 条见 `框选修正端到端实测记录.md`，含五类说明方式的逐条结果、三条分发用例、十三处缺陷与用量。

两份记录都写明本次没有截图：浏览器面板未显示，页面不合成帧，以 DOM 读数与库内容为准；指针捕获未验证。

## 五、复查清单

- 是否符合规格.md 的范围与业务规则：符合。五类说明方式与口径按规格实现
- 是否引入规格之外的功能：三件补齐能力（构件可写、排除记录落库、构件带图像坐标）属规格第三节范围内的前置依赖，不算规格外
- 相关文档是否同步更新：界面文档表 10 实现状态、分发测试用例集第 3 节、路线图三处已同步
- 是否存在与本单元无关的重构：无

## 六、结论

十条全部通过。第二节三处差异已按已定口径实现，本单元已关闭。
