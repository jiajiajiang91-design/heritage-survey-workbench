# 成功归档与用语层 · 验收

条件在开工前写定，逐条可判定。

## 一、验收条件

| 序 | 条件 | 结果 |
|---:|---|---|
| 1 | 三个演示项目打开后：缺失资料 0、未关闭问题 0、模型未知项 0、检查全部通过、交付带复核签发记录、成果状态显示为已签发可正式交付 | **通过后修订展示口径**，三个演示包缺失资料 0、未关闭问题 0、检查通过，并带正式构建环境生成的签发记录（`check-demo-packages` 输出见证据 `18_成功归档与用语层/实测记录.md` 第二节）。2026-08-25 首次使用复核发现，展示数据与可正式交付并列会造成责任边界冲突，因此界面改为流程演示签发记录、展示流程已归档和不可用于工程交付。构造样板模型保留 909 条构件级翻译近似说明，由随包记录逐项接受，界面显示为建模说明，不再作为待确认项。 |
| 2 | 项目列表顺序为高都、团队样板、Dai Loy，三张卡都有封面 | **通过**，列表顺序高都、构造样板、Dai Loy（按项目建立日期升序）；高都与 Dai Loy 封面为现场照片，构造样板封面为第一张图纸 SVG（截图 `17_十九屏重做/截图/P01_项目列表.png`） |
| 3 | 高都的实测记录有测量人、日期、仪器、方法，数值与照片比例一致，资料里注明演示设定值 | **通过**，`03_公开实测样本/高都玉皇庙/现场实测记录.csv` 28 条，逐条有部位、数值、仪器、方法、测量人、记录人、日期；表尾与资料权利声明注明演示设定值；数值与照片比例一致（柱高 3400、明间 3600、脊高 7200 等沿用照片比例推算值并升为实测口径） |
| 4 | 本机新建项目仍不能签发：命令服务拒绝本机环境的签发记录，界面没有签发入口 | **通过**，`LocalAuthorization` 拒绝 `RecordReviewSignoff`（`authorization.test.ts`）；界面上签发按钮为禁用态并注明签发须在正式环境完成；演示包构建走 `FormalEnvironmentAuthorization`，仅 `tools/build-demo-packages.mjs` 使用 |
| 5 | 《界面用语表》进界面文档；界面正文不出现阻断、证据、代理成果、稳定键、英文阻断码、原始文件名作主行；测试锁住 | **通过**，界面文档新增第八节表 13 界面用语表；工作台源码由 `wording.test.ts` 扫描，禁用词（证据、阻断、代理成果、稳定键、存疑、几何、事实等十四个）在注释以外出现即失败；成果列表主行为中文成果名，文件名降为次行 |
| 6 | 在项目里提问得到由模型生成的有内容回答；助手建议卡由模型生成并带依据；面板结构与 v4 一致 | **通过**，高都项目实测三问三答（几份资料、哪张 1:50、下一步做什么）均由模型按项目现状回答，内容与真实数据一致；助手建议卡由模型生成并带依据；面板为 v4 结构：任务状态、助手建议、消息流、底部输入区（问答记录见证据第三节） |
| 7 | 图片、PDF、SVG、DXF 在页内可看，PDF 无浏览器工具条；DWG 有下载与说明 | **通过**，图片与 SVG 缩放平移查看，PDF 用 pdf.js 本地渲染（JBIG2 扫描件经同源 wasm 解码器正常显示，无浏览器工具条），DXF 解析 4907 个图元渲染为线图；成组图纸右卡与成果归档对话框都可页内查看；DWG 有下载与闭源格式说明（截图 V_* 四张） |
| 8 | 十九屏截图无卡内滚动条；中栏整体滚动 | **通过**，第三轮重截的十九屏截图无卡内滚动条，中栏一根整体滚动条（`ws-center`）；卡底操作行贴住卡底 |
| 9 | `run-all --full` 八项全过；演示矩阵二十一格全满 | **通过**，`run-all --full` 八项全过；演示矩阵二十一格全满；`check-demo-packages` 三包一致 |

## 二、自动检查

- `pnpm --filter @gujian/workbench test`、`pnpm run typecheck`
- `node tools/check-demo-packages.mjs`、`node tools/check-demo-matrix.mjs`
- `node .claude/skills/quality-check/scripts/run-all.mjs --full`

## 三、人工检查

1. 三个演示项目逐屏走一遍，读每句说明文字，确认没有领域词。
2. 助手里问三个问题：项目有几份资料、哪张图是 1:50、下一步做什么。
3. 打开一份 PDF 与一张 DXF 图纸，缩放查看。

## 四、复查清单

- 是否符合规格.md 的范围与业务规则
- 是否引入规格之外的功能
- 相关文档是否同步更新，包括核心 PRD、界面文档与路线图
- 是否存在与本单元无关的重构

## 五、结论

九条全部通过，一条（第 1 条）结论达到但与条件原文不同：构造样板的翻译近似说明由复核签发接受而不是清零，原因写在上表。

复查清单四问：范围与规格一致；没有引入规格之外的功能（演示包更新提示是演示包重做的必然配套，否则老用户看不到新包）；界面文档表 13、路线图、索引、偏离清单已同步；与本单元无关的重构没有。

复盘五问：路线图下一步是单元 10 第三代动作，前置不变；用语表已进界面文档并由测试锁住；签发只能来自正式环境这条规则写进了规格与代码注释；技术债新增两条（见路线图）；截图与巡检脚本已在单元 08 沉淀，本单元复用。单元关闭。
