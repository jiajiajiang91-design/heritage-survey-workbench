# 成功归档与用语层 · 计划

六组，每组跑本组验证再进下一组，提交不推远端。

| 组 | 内容 | 验证 |
|---|---|---|
| 一 · 签发记录 | 领域层 `ReviewSignoff` 与快照字段；命令 `RecordReviewSignoff`（只接受正式环境记录）；交付阻断、成果资格、资格标签按签发记录派生 | 领域与应用测试；阻断测试加"有签发记录时三条正式阻断解除" |
| 二 · 演示包 | 高都补三张照片与实测记录表，尺寸改实测已确认，问题清空，形制补齐山面、飞椽、门窗、基础、敞廊依据；团队样板与 Dai Loy 同口径补齐；三个项目加签发记录；重建三个包与清单；列表顺序与封面 | `tools/check-demo-packages.mjs`、演示矩阵；三项目巡检无阻断、无未知项、无缺失资料 |
| 三 · 用语表 | 写《界面用语表》进界面文档第四节；标签表与各屏文案按表改；说明文字一句 | `wording` 测试：界面源码不含表里禁用的词 |
| 四 · 助手 | 服务端回答问题走模型第二次调用；新增建议接口由模型生成；面板按 v4 重排 | 脚本提问得到有内容的回答；建议卡有内容与依据 |
| 五 · 查看器与版式 | 统一文件查看器（图片、PDF、SVG、DXF、GLB）；中栏整体滚动 | 三项目逐屏截图无卡内滚动条；PDF 与 DXF 页内可见 |
| 六 · 收口 | 十九屏重截、验收填写、路线图、偏离清单、全量检查 | `run-all --full` 八项 |

## 完成定义

验收九条全部判定且通过；`run-all --full` 八项全过；三个演示包重建并通过 `check-demo-packages`；路线图状态更新；按复盘清单复盘。
